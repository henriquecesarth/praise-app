# Project Memory

## Project Identity

- Nome: LouvAIO (anteriormente Praise App).
- Propósito: gestão web e PWA de ministérios de louvor, seus integrantes, repertório, escalas, cifras inteligentes, planos, quotas e faturamento recorrente.
- Identidade Visual Oficial: Verde escuro `#0F2A1F`, Terracota `#B85A3C`, Creme `#F5EFE6`, Preto `#121212`. Tokens centralizados em `web/src/styles/louvaio-brand.css`, `web/src/theme/louvaioTheme.ts` e `web/src/index.css`.
- Stack: React 18 + TypeScript + Vite/PWA + TailwindCSS v4; Node.js + TypeScript + Express 5; Firebase Authentication + Cloud Firestore; Asaas SaaS Gateway.
- Entry points: web/src/main.tsx, web/src/App.tsx, backend/src/server.ts e backend/src/app.ts.

## Architecture

Monorepo com dois pacotes npm independentes (`backend` e `web`). A SPA usa web/src/api.ts para consumir a API REST. O backend segue predominantemente route → middleware → controller → service → repository → Firestore. Não há workspace/package raiz.

## Important Components

- App.tsx: sessão, ministério ativo, navegação, sidebar institucional LouvAIO e estado principal da SPA.
- api.ts: URL base, JWT em localStorage, fetch e mapeamento de contratos.
- app.ts: middleware e montagem das rotas Express.
- server.ts: inicialização do servidor Express e ciclo de vida do BillingReconcilerWorker.
- unifiedConfig.ts: leitura/validação Zod de ambiente e defaults.
- firebase.ts: Firebase Admin, Firestore e Auth.
- middleware/auth.ts, rbac.ts, platform-admin.ts e quota-enforcement.ts: segurança, papéis, autoridade da plataforma e quotas operacionais.
- repositories/: fonte dos nomes de coleções e comportamento de persistência Firestore.
- theme/louvaioTheme.ts: mapeamento de cores e caminhos de assets oficiais de branding.

## Important Domain Concepts

Ministry é a fronteira principal de tenant. Usuários podem ser admin ou member; também há funções musicais separadas. Recursos incluem membros, convites PR-*, equipes, funções, classificações, artistas, músicas/versões, pastas, escalas, comentários, modelos de roteiro, liturgias, cifras, assinaturas, quotas e transações de faturamento.

### Commercial Structure & Plans

- 6 planos definidos em `backend/src/config/plans.config.ts`:
  - `free`: 10 membros / 50 músicas (R$ 0,00)
  - `lite`: 20 membros / 100 músicas (R$ 14,90/mês, 10% OFF anual)
  - `lite_plus`: 30 membros / 150 músicas, apresentado como Lite+ (R$ 24,90/mês, 10% OFF anual)
  - `essential`: 40 membros / 200 músicas (R$ 34,90/mês, 10% OFF anual), suporta add-on (+10 membros por bloco, máx 4 blocos = 80 membros)
  - `pro`: 100 membros / 500 músicas (R$ 89,90/mês, 10% OFF anual), suporta add-on (+10 membros por bloco, máx 10 blocos = 200 membros)
  - `premium`: 300 membros / 1.500 músicas (R$ 214,90/mês, 10% OFF anual)
- Preços operam deterministamente em centavos inteiros (`BRL`). Desconto anual de 10% calculado via fórmula padronizada. Detalhes em `docs/product/plans-and-limits.md`.

## Integrations

- Firebase Admin SDK: Auth e Firestore.
- Firebase Identity Toolkit REST: login por e-mail/senha quando FIREBASE_WEB_API_KEY existe.
- Asaas Payment Gateway: integração desacoplada via `AsaasBillingProvider` para assinaturas recorrentes no cartão, checkouts hospedados e webhooks.
- Vercel: manifests independentes para frontend e backend.
- Google Fonts e html2pdf.js: carregados por CDN no HTML.
- Supabase: dependência/stub legado, não é persistência ativa.

## Persistence

Cloud Firestore é a fonte persistente ativa. Nomes de coleções e relações estão documentados em `docs/architecture/integrations.md`. Não há migrations ou schema SQL versionado.

## Configuration

Variáveis de ambiente reconhecidas pelo backend via `unifiedConfig.ts`:
- Servidor e Segurança: `PORT`, `NODE_ENV`, `JWT_SECRET`, `CORS_ORIGIN`, `DEFAULT_MINISTRY_ID`, `PLATFORM_ADMIN_SECRET`
- Firebase: `FIREBASE_PROJECT_ID`, `FIREBASE_CLIENT_EMAIL`, `FIREBASE_PRIVATE_KEY`, `FIREBASE_DATABASE_URL`, `FIREBASE_WEB_API_KEY`
- Faturamento e Gateway: `BILLING_TIMEZONE`, `BILLING_RECONCILIATION_ENABLED`, `BILLING_RECONCILIATION_INTERVAL_MINUTES`, `WEB_APP_URL`, `BILLING_PUBLIC_API_URL`, `ASAAS_API_URL`, `ASAAS_API_KEY`, `ASAAS_WEBHOOK_TOKEN`, `ASAAS_ENVIRONMENT`
- Frontend: `VITE_API_URL`

Nunca registrar valores secretos no repositório.

## Testing

O backend usa Vitest para testes unitários e de integração cobrindo motor de quotas, serviços de assinatura, concessões cortesia (complimentary), isolamento de tenant, segurança contra IDOR, RBAC, autenticação/tokens, otimização de consultas/repositórios com cursor/agregação, controllers e serviços de billing/checkout com concorrência e idempotência atômica. O web usa Vitest/Testing Library para testes unitários/componentes e Playwright para jornadas E2E móveis com matriz de viewports e temas light/dark via canal Chrome nativo (jornadas E2E interceptam a API e usam fixtures locais sem escrita persistente). Ambos os pacotes são validados por build TypeScript (`tsc`). O lint backend está declarado, mas não operacional.

## Operational Commands

    cd backend && npm ci
    cd backend && npm run dev
    cd backend && npm run build
    cd backend && npm test
    cd backend && npm start

    cd web && npm ci
    cd web && npm run dev
    cd web && npm run build
    cd web && npm test
    cd web && npm run test:e2e
    cd web && npm run preview

## Important Decisions

- Código atual prevalece sobre documentação histórica.
- Identidade visual oficial LouvAIO adotada com paleta Verde/Terracota/Creme/Preto e tokens centralizados (`docs/decisions/2026-08-28-louvaio-visual-identity.md`).
- Contenção responsiva sem uso de `overflow-x: hidden` global.
- Sistema de planos, quotas e entitlements operado com autoridade no backend (`backend/src/config/plans.config.ts`) e consumido dinamicamente pelo frontend.
- Separação estrita entre `BillingStatus` (financeiro) e `administratively_suspended` (administrativo/fraude).
- Resolução dinâmica de `accessMode` (`normal`, `grace`, `restricted_over_limit`, `suspended`) sem dependência de jobs ou escrita prévia.
- Pre-Launch Cost & Scalability Optimization: Consultas de repertório operam com paginação estável por cursor (`startAfter(updated_at, doc_id)`), ordenação composta (`updated_at DESC, __name__ DESC`), DTO enxuto `SongSummary` (-90% egress de listagem), agregação de contagem (`count().get()`), eliminação de $N+1$ em integrantes e pastas através de batch lookup único via `db.getAll()`, suporte a `olderCursor` em comentários e índices declarados em `backend/firestore.indexes.json`.
- Authentication & Authorization Security Hardening: Validação de login obrigatória no Identity Toolkit; suporte dual assíncrono de tokens (JWT assinado + Firebase ID Token); rotas e repositórios protegidos com isolamento multitenant estrito e checagem anti-IDOR; proteção das rotas de cifras inteligentes (`smart_chords`) com filtro por `user_id`; mitigação de account takeover em integrantes e aplicação da regra do último administrador ("Last Admin Rule").
- Billing, Checkout & Subscription Automation: Gateway Asaas integrado através de interface desacoplada `BillingProvider`; separação estrita de autoridade (gateway controla estado financeiro, LouvAIO controla direito de uso e quotas); transições de plano isoladas na coleção `billing_plan_changes` para não sobrescrever a assinatura ativa antes da confirmação do pagamento; webhooks transacionais com idempotência atômica garantida por Firestore Transaction na coleção `billing_webhook_events`; validação de valor pago (Amount Validation); proteção contra eventos fora de ordem (Out-of-order sequence guards); suporte integral a planos cortesia (`subscription_mode = 'complimentary'`) concedidos por autoridade da plataforma (`PLATFORM_ADMIN_SECRET`) com zero chamadas ao Asaas; rotina de limpeza de cobranças futuras PENDING da assinatura anterior (`cleanupFuturePaymentsFromPreviousSubscription`) com cutoff date comercial e flag `financial_attention_required` para mitigação de race condition; preservação absoluta de dados em downgrades/cancelamentos aplicando período de adaptação (`grace`).
- Webhook Event Lifecycle Semantics (Phase 3B.3 Hardening — 2026-09-03, CORRIGIDO 2026-09-03): `BillingWebhookEvent` lifecycle != `BillingTransition` lifecycle. Um webhook event representa UMA entrega do provider; uma BillingTransition representa a saga financeira longa. O `processing_status = 'processing'` é estritamente transitório — somente durante execução ativa do handler. Todo caminho de retorno de `processScheduledPaidRenewalSettlement` que representa uma avaliação terminal do domínio deve chamar `markWebhookEventProcessed` antes de retornar, independentemente de `processed: false`. **Não existe lease interno de webhook; não existe stale-reclaim automático; a única forma de re-executar um webhook é via provider redelivery.** Classificação canônica dos caminhos: (A) Consumido, sem mudança de estado → `markWebhookEventProcessed('processed')` [renewal_payment_not_settled, strategy_mismatch, SECOND_CYCLE_PAYMENT, scheduled_event_acknowledged]; (B) Transitoriamente incompleto, saga pertence ao reconciler → `markWebhookEventProcessed('processed')` + transition permanece live [PAYMENT_NOT_FOUND: reconciler usa `future_provider_payment_id` para poll autônomo; SOURCE_CUTOVER_NOT_COMPLETED: reconciler executa cutover→settlement; ACTIVATION_COMPLETION_GATE_FAILED: reconciler re-executa gate com state fresco]; (C) Financial attention persistida na transition → `markWebhookEventProcessed('processed')` [SOURCE_SUBSCRIPTION_STILL_ACTIVE: grava `financial_attention_required=true` antes de finalizar; todos os mismatches que setam `financial_attention_required=true` — o bloqueio vem do campo da transition, não do status do evento]; (D) Webhook de recurso errado → `markWebhookEventProcessed('ignored')` [WRONG_PAYMENT_ID, WRONG_TARGET_SUBSCRIPTION]. `claimTransitionForReconciliation` bloqueia somente transitions com `financial_safety_status = 'attention_required'` (mismatch real), não initial purchases com resultado de checkout incerto (`financial_attention_required=true` mas `financial_safety_status` ausente ou `live`). HTTP 5xx somente para falhas reais de processamento da delivery; HTTP 2xx nunca deixa o evento permanentemente em processing.
- Firebase/Firestore é a implementação ativa; referências antigas a Supabase/Flutter não descrevem este checkout.
- Não existe contrato HTTP uniformizado; mudanças devem preservar respostas locais.
- Inconsistências confirmadas ficam catalogadas em docs/system-status.md e exigem tarefas próprias.

## Known Constraints

- Testes de concorrência física de transações sob Firestore Emulator: NOT YET VERIFIED (validado com mocks unitários). Sem CI/CD, Docker, migrations, formatter ou esquema Firestore versionado.
- Componentes web e o cliente api.ts concentram muito comportamento.
- Alias groups/ministry e campos snake_case/camelCase coexistem por compatibilidade.
- Versão mínima suportada de Node/npm: Unknown / Not yet verified.

- Política de Transições de Assinatura V1 (ADR 2026-09-01 Revisado): Period-Paid Principle & Separação de Estados (`Asaas subscription != LouvAIO entitlement`). Todo período pago pertence ao cliente até `current_period_end`. Upgrades de plano utilizam a arquitetura `RECURRENCE_FIRST` (autorização da renovação futura do plano alvo em `current_period_end` + oferta opcional de compra de acesso antecipado via ajuste proporcional). Downgrades, reduções de add-ons e trocas de periodicidade (mensal/anual) permanecem agendados para `current_period_end` sem perda antecipada de entitlement nem cobrança sobreposta. Price lock na solicitação.
- Slot Lifetime Invariant: O active transition slot permanece HELD em `pending_future_authorization`, `future_target_prepared` e `scheduled` enquanto existir obrigação financeira futura viva. A Phase 3B.2 não libera o slot após o old cutover. Liberação de slot ocorre estritamente em condição terminal comprovadamente segura (`completed + safe_terminal` ou cancelamento/falha compensado).
- Billing Transition V1 Status:
  - Phase 1 (Modelo de Persistência, Slot Ativo Determinístico, Invariantes e Attempts): COMPLETE.
  - Phase 2 (Domain Services, Validações Pré-Checkout, Quotas e Quotes V1): COMPLETE.
  - Phase 3A (Free -> Paid V1 Initial Purchase): CLOSED AND SANDBOX HOMOLOGATED (Saga completa ponta a ponta homologada no Asaas Sandbox com checkout hospedado, webhooks reais, dupla camada de idempotência de evento e transação financeira, proveniência temporal exata com `paid_billing_date`, liberação determinística do slot ativo e persistência de `safe_terminal`).
  - Billing Sandbox Bootstrap: READY (Versão 1.1 com isolamento de ambiente, ciclo de vida de processos filhos, supervisão contínua do Quick Tunnel e sincronização idempotente de webhooks).
  - Phase 3B.1 (Paid -> Paid Target Recurrence Preparation): COMPLETE, HARDENED & PROVIDER CONTRACT ALIGNED (Cadeia canônica attempt -> known provider checkout ID -> GET /v3/payments?checkoutSession -> target subscription -> first payment; sem dependência de endpoints não documentados do Asaas; sem assunção de externalReference na assinatura; recovery authorities documentadas; detecção de ambiguidade fail-closed; Target Ready Gate desacoplado de nextDueDate; conversão monetária determinística; slot retido; zero mutações na assinatura de origem e zero ativações de entitlement).
  - Phase 3B.2 (Paid -> Paid Source Recurrence Cutover & Scheduling): COMPLETE & CLEAN END-TO-END SANDBOX HOMOLOGATED (Transição da recorrência de origem: future_target_prepared -> awaiting_old_inactivation -> scheduled; datas comerciais persistidas na criação da transição sem reparos manuais; inativação da source via PUT status INACTIVE; sobrevivência de cobrança pendente comprovada em Sandbox e limpa cirurgicamente pelo LouvAIO com verificação all-status; target payment permanece intacto e PENDING; transição avança para scheduled via BillingReconcilerWorker; runtime entitlement LouvAIO retido estritamente na source Lite até renewal-boundary; slot de transição HELD em scheduled; cancelamento explícito seguro de checkout transita para status canceled com safe_terminal e liberação de slot; idempotência e proteção contra eventos atrasados; target PENDING não gera receita de BillingTransaction).
  - Phase 3B.3A (Scheduled Renewal Settlement, Immutable Snapshot Activation & Crash-Safety Hardening): COMPLETE & CLEAN END-TO-END SANDBOX HOMOLOGATED (Scenario A homologado de ponta a ponta no Asaas Sandbox com Hosted Checkout real, webhooks reais e reconciliador assíncrono. Two-Gate Model cumulativo validado; snapshot de cotas imutável; ordem canônica estrita target prepared -> source cutover -> source INACTIVE + pending cleanup -> supersede completed -> scheduled -> renewal settlement -> fresh source safety check -> target quota promotion -> BillingTransaction -> BillingSubscription canônica min_x_asaas -> local activation completion gate -> completed safe_terminal -> slot release; mitigação comprovada contra webhook race condition e concorrência com o worker; slot retido até a convergência total; 45 testes dedicados e 493 testes no backend passando).
  - Phase 3B.3B (Renewal Failure, 7-Day Civil Grace & Delinquency Recovery): COMPLETE, AUTOMATED TEST SUITE PASSING & PROVIDER UNPAID BEHAVIOR SANDBOX VALIDATED (Modelo civil estrito de 7 dias [grace_start_billing_date, grace_end_billing_date) em America/Sao_Paulo derivado deterministicamente de effective_billing_date via addCommercialDays; cálculo sem 168h/ms/UTC; entrada write-once grava grace_started_at, datas de carência e grace_entitlement_snapshot capturando o runtime entitlement pre-boundary real de ministry_subscriptions; plano alvo não quitado nunca concede direitos durante carência; quitação dentro da carência destrava ativação target reutilizando o pipeline hardened 3B.3A com novo ciclo iniciando na fronteira original e paid_billing_date preservando a data real do evento do provedor; expiração da carência comuta status para expired e resolveAccessMode para restricted_over_limit com preservação absoluta de dados [zero deletions]; transição permanece scheduled e slot permanece HELD; quitação na/após expiração não auto-ativa e sinaliza RENEWAL_SETTLED_AFTER_GRACE_REQUIRES_POLICY com slot retido documentando gap de delinquency recovery; reconciliador assíncrono e webhook compartilham a mesma máquina de estados determinística; Scenario B Sandbox: PAYMENT_OVERDUE recebido, evaluated corretamente como pre-boundary, domínio confirmado, grace não iniciada por clock real real limitado; civil grace E2E limitado por clock real sem IClock abstraction — classificado como limitação legítima; 29 testes dedicados + 29 testes de 3B.3A + 493 testes backend passando. Webhook Lifecycle Hardening: COMPLETE — ver decisão de Webhook Event Lifecycle no Important Decisions.).
  - Phase 3C (Early Activation):
    - Phase 3C.1 (Domain, Eligibility & Proration): COMPLETE (Classificação determinística pure_upgrade via snapshots travados, cálculo de pró-rata em centavos inteiros sem ponto flutuante, invariantes de data e lock de preços).
    - Phase 3C.2 (Detached Checkout Preparation): COMPLETE & HARDENED (Preparação de checkout avulso com attempt `reserved` -> `attempting`, TTL determinístico com regra estrita de mínimo 10 minutos [10-1440 min] conforme contrato do provedor, URL canônica oficial /checkoutSession/show?id=, blindagem de timeouts/incertezas com quarentena e retenção de obrigação viva).
    - Phase 3C.3 (Tenant-Scoped Early Activation API & Final Hardening): COMPLETE (Rotas canônicas POST /quote e POST /checkout sob requireMinistryRole('admin'); cotação pura baseada em snapshots travados sem chamada ao gateway; persistência atômica CAS de quote com revalidação transacional de prontidão financeira, fresh economic binding e proteção contra TOCTOU; semântica HTTP 201 para sucesso conhecido e HTTP 202 com status creation_verification_pending para OUTCOME_UNCERTAIN sem checkoutUrl e sem sugerir retry cego; blindagem anti-IDOR da cotação; autoridade exclusiva do backend para callbacks e rejeição de injeção de campos financeiros no body; 56 testes dedicados e 699 testes no backend passando).
    - Phase 3C.4 (Adjustment Settlement, Idempotent Webhook, Immediate Entitlement Convergence & Stale Ledger Patch): COMPLETE & HARDENED (Máquina de estados canônica `processEarlyActivationAdjustmentSettlement` com roteamento desacoplado; isolamento estrito entre ajuste avulso e recorrência futura; persistência write-once de `successful_early_adjustment_provider_payment_id` e data de liquidação com fail closed; registro de `BillingTransaction` canônica de tipo `prorated_early_activation_adjustment` mantendo co-existência limpa de 2 transações em renovação posterior; promoção imediata de cotas em `SubscriptionService` via `applyLockedEntitlementSnapshot` com preservação inalterada do ciclo comercial de origem `[current_period_start, current_period_end]`; Commercial Boundary Guard com retenção de slot e zero auto-refund caso liquidado na/após boundary; Local Completion Gate validando 7 invariantes em leitura fresh antes de transicionar `early_activation_status` para `activated`; transição permanece `scheduled`, status de segurança permanece `live` e slot ativo permanece `HELD`; compatibilidade nativa com carência da Phase 3B.3B preservando cotas do alvo ativado antecipadamente; Crash Matrix A-F provada com recuperação determinística em todas as fronteiras de persistência; monotonicidade estrita de eventos terminais contra regressões; proteção contra conflito write-once em checkout ID e payment ID; isolamento de tentativas stale: STALE PROVIDER CHECKOUT MATERIALIZATION IS A FINANCIAL SAFETY CONFLICT; Stale Settled Payment Ledger Patch: A SETTLEMENT EVENT IS AN IMMUTABLE HISTORICAL FINANCIAL FACT, EVEN IF THE FINANCIAL STATE IS LATER REVERSED [a ocorrência da liquidação é um fato histórico financeiro imutável, mesmo que posteriormente exista refund, chargeback ou outra reversão; stale settled early-adjustment payment → canonical BillingTransaction → financial attention → no entitlement → no auto-refund; transação salva idempotentemente com attempt_id = att_old.attempt_id, slot mantido HELD, att_current intacto; transição ativada mantém entitlement intacto e grava BillingTransaction do pagamento histórico em atenção; atenção ativa bloqueia convergência automática posterior do current payment; conflito de dados na transaction falha fechado com FINANCIAL_TRANSACTION_CONFLICT]; Uncertain Create Webhook Recovery implementada e provada sem retry cego; 54 testes dedicados cobrindo liquidação, crash matrix A-F, stale checkout safety e stale settled payment ledger; 753 testes no backend passando).
    - Phase 3C.5A (Early Activation Known-Checkout Reconciliation Worker, Global V1 Scheduler Fairness, Legacy Normalization & Cross-Phase Coverage): COMPLETE & CHECKPOINTED (Reconciliador assíncrono `reconcilePaidToPaidEarlyActivationAdjustment` integrado ao `BillingReconcilerWorker`; EARLY ACTIVATION ROUTING: opera estritamente quando `item.execution_strategy === 'scheduled_paid_transition'` com `item.transition_status === 'scheduled'` e `item.early_activation_status` pendente com `provider_checkout_id` já conhecido via `listPaymentsByCheckoutSession(checkoutId)`; submete deterministicamente os pagamentos observados à state machine canônica da Phase 3C.4 `processEarlyActivationAdjustmentSettlement`; MULTIPLE PROVIDER PAYMENT RECORDS FOR ONE DETACHED OBLIGATION ARE A FINANCIAL SAFETY CONFLICT; SETTLED MEMBERS OF AN AMBIGUOUS SET ARE STILL LEDGER FACTS: qualquer cobrança liquidada em conjunto ambíguo entra no ledger canônico como `BillingTransaction` idempotente com ID `${provider}_${provider_payment_id}`, acionando `EARLY_ADJUSTMENT_MULTIPLE_PROVIDER_PAYMENTS` sem concessão de entitlement, slot HELD e zero refund; proteção contra `FINANCIAL_TRANSACTION_CONFLICT` em colisão de amount/date; checagem estrita de Slot Ownership antes de conceder direito de uso com fail closed; quarentena estrita mantida para attempts incertos sem checkout ID conhecido [zero blind retry, zero discovery inventado]; tratamento de zero payments como benigno com retenção de slot HELD; reconciliação de tentativas stale com registro canônico no ledger contábil sem entitlement; Commercial Boundary Guard acionando atenção financeira sem auto-estorno; tratamento de pagamentos pendentes, vencidos, estornados e contestados; classificação rigorosa de falhas do provedor: transitórias [timeouts/network/5xx -> `provider_read_failure`] vs operacionais de autenticação/permissão [401 -> `provider_auth_failure_401`, 403 -> `provider_auth_failure_403`] vs respostas malformadas [`malformed_provider_response` fail closed]; GLOBAL V1 RECONCILIATION SCHEDULER: `getV1TransitionsNeedingReconciliation` atende integralmente todas as fases ativas no runtime (3A Initial Purchase, 3B Paid-to-Paid e 3C Early Activation); SINGLE SOURCE OF RECONCILABLE STATES: constante canônica `V1_RECONCILABLE_TRANSITION_STATUSES` em `billing.types.ts` mapeia exatamente os 5 status operacionais vivos (`pending_initial_purchase`, `pending_future_authorization`, `future_target_prepared`, `awaiting_old_inactivation`, `scheduled`); estados terminais (`completed`, `canceled`, `superseded`, `failed`, `safe_terminal`) são estritamente excluídos antes do limit; BOUNDED NORMALIZATION PASS WITH DURABLE CURSOR & SCAN-START CAS: varredura em Document ID (`FieldPath.documentId() ASC`) com cursor durável persistido em `billing_schedulers/normalization_{provider}` cobrindo todos os 5 status mais `attention`; wrap para `null` condicionado a `currentPersistedCursor === expectedStartCursor` com avanço monotônico; fresh transaction check atômico que JAMAIS sobrescreve timestamp real de agendamento; zero mutação em campos de negócio e zero mutação em `updated_at`; novas transições nascem com `last_reconciled_at: null` por padrão; ATTENTION TERMINAL STARVATION GUARD: consultas de atenção separadas pelos status em `V1_RECONCILABLE_TRANSITION_STATUSES` com `financial_attention_required === true`, prevenindo que registros terminais históricos consumam a capacidade do lote antes de registros operacionais vivos; fair round-robin interleaving multi-bucket com isolamento estrito de buckets operacionais (`filterHealthy`); exatamente 2 composite indexes versionados em `backend/firestore.indexes.json` (`provider + transition_status + last_reconciled_at + __name__` e `provider + transition_status + financial_attention_required + last_reconciled_at + __name__`); 100 testes dedicados cobrindo integralmente as matrizes da 3C.5A, Scan-Start CAS, Wrap Concurrency, Attention Starvation Guard, Cross-Phase Test Matrix, 100/125 legacy, mixed, restart, crashes e index contracts; 853 testes no backend e 48 testes no web passando).
    - Phase 3C.5B (Early Activation Checkout Expiry & Cancellation Cleanup — Terminal Monotonicity & Lifecycle Hardening): COMPLETE & HARDENED (Separação canônica estrita entre expiração natural e cancelamento explícito: CHECKOUT EXPIRATION AND EXPLICIT CANCELLATION ARE DISTINCT TERMINAL OUTCOMES; TERMINAL CHECKOUT EVENTS ARE MONOTONIC; A LATE CONFLICTING TERMINAL EVENT DOES NOT SILENTLY REWRITE PROVIDER HISTORY; MONEY EVIDENCE REMAINS AUTHORITATIVE AS A FINANCIAL FACT EVEN AFTER TERMINAL CHECKOUT STATE; NATURAL EXPIRY DOES NOT REQUIRE POST CANCEL — checkouts expirados pelo relógio local TTL [now >= attempt.expires_at] NÃO invocam POST /v3/checkouts/{id}/cancel automaticamente e aguardam evidência terminal oficial do provedor; CHECKOUT_EXPIRED IS AN OFFICIAL PROVIDER EVENT AND IS THE TERMINAL PROVIDER EVIDENCE FOR NATURAL EXPIRY; CHECKOUT_EXPIRED é mapeado no adapter Asaas e processado pela máquina de estados canônica; CHECKOUT_EXPIRED com zero pagamentos atesta expiração segura [status = 'expired', provider_session_terminal = true, failure_classification = 'session_expired', expiry_confirmed_at] sem transitar para canceled; CHECKOUT_EXPIRED com pagamentos materializados: CONFIRMED/RECEIVED segue máquina de liquidação; PENDING/OVERDUE bloqueia substituição com falha fechada; múltiplos aciona financial attention no ledger; falha de leitura no gate pós-expiração mantém verificação financeira pendente [uncertain_expired]; Cancelamento explícito via provedor [provider.cancelCheckout] é restrito estritamente a momentos pré-expiração [now < attempt.expires_at] com protocolo de preflight payments -> CAS intent attempting -> POST cancel -> postflight payments -> safe_canceled; EXPLICIT CANCEL INFRASTRUCTURE EXISTS, BUT NO AUTOMATIC PRODUCT TRIGGER CURRENTLY INVOKES IT; Adaptação HTTP 200 normalizada deterministicamente para { success: true, status: 'CANCELED' }; ZERO PAYMENTS DO NOT PROVE THAT AN UNCERTAIN CANCELLATION SUCCEEDED: cancel_state uncertain + zero payments permanece em quarentena incerta no worker sem virar canceled; CHECKOUT_CANCELED + PAYMENT SAFETY GATE é a autoridade canônica necessária para recuperar cancelamento incerto com segurança; SAFE EXPIRY E SAFE CANCELLATION PRESERVAM A TRANSIÇÃO SCHEDULED, SLOT ATIVO HELD E RECORRÊNCIA ALVO INTOCADA; Sem auto-estorno nem deleção de dados; Tentativas expiradas são distintas no histórico de canceladas [provider_expired_at, expiry_confirmed_at vs cancellation_confirmed_at]; Nova cotação após safe expired é permitida antes da fronteira comercial com recálculo de pró-rata; Quarentena contra cotação prematura antes de evidência terminal do provedor; Conflito de eventos terminais atrasados [expired -> canceled, canceled -> expired] é idempotente sem regredir ou reescrever histórico; Pagamento tardio liquidado após término do checkout [expired/canceled + CONFIRMED/RECEIVED] grava BillingTransaction como fato imutável e aciona atenção financeira sem conceder entitlement e sem auto-estorno; Lost CHECKOUT_EXPIRED webhook gap: known limitation / fail closed; 47 testes dedicados na matriz da Phase 3C.5B e 900 testes no backend passando).
    - Phase 3C.6 (Early Activation End-to-End Sandbox Homologation): COMPLETE & SANDBOX HOMOLOGATED (Homologação completa e limpa concluída contra o Asaas Sandbox oficial em 2026-09-04 sem alterações no código de produção; autoridade canônica comprovada com separação estrita de cenários):
      - *Scenario A3 (Authoritative Canonical Entitlement E2E Proof)*: Homologação definitiva e canônica de ponta a ponta no Asaas Sandbox. Utilizou exclusivamente a autoridade mais alta de produção (`buildTransitionCommercialSnapshot`, `buildBillingTransitionV1Record`, `createTransitionAndClaimSlot`) sem crafting manual de snapshots nem fixtures unitárias. Provou no runtime real o catálogo de produtos (`PLANS_CATALOG`): Lite (20 membros / 100 músicas) -> Essential (40 membros / 200 músicas), com snapshot econômico (R$ 14,90 -> R$ 34,90). Estado inicial em `SubscriptionService.getSubscriptionSummary` comprovado como Lite 20/100 (`quotas.members = 20`, `quotas.songs = 100`). Cotação pró-rata com relógio real em data comercial: 1267 centavos (R$ 12,67 para 19/30 dias). Exatamente 1 detached checkout (`early_activation`) gerado no Asaas Sandbox. Pagamento hosted real efetuado com cartão Sandbox. Liquidação contábil operada por `PAYMENT_CONFIRMED`. Exatamente 1 `BillingTransaction` canônica gravada no ledger (`prorated_early_activation_adjustment`). Snapshot aplicado via `applyLockedEntitlementSnapshot` com quotas bloqueadas persistidas em `ministry_subscriptions` (`locked_member_quota = 40`, `locked_song_quota = 200`). `SubscriptionService.getSubscriptionSummary` final retornou exatamente Essential 40/200 (`quotas.members = 40`, `quotas.songs = 200`). Invariantes preservados: `current_period_end` e `effective_billing_date` inalterados, transição mantida `scheduled`, slot global determinístico retido em `HELD`, recorrências de origem e destino inalteradas. Segunda passada do reconciliador 100% idempotente (`already_activated` / NO-OP, 1 transação contábil).
      - *Scenario A2 Quota Fixture Contamination Disclosure*: O Scenario A2 corrigiu integralmente a contaminação de slot do A1, usou slot canônico e comprovou os mecanismos financeiros de checkout, pagamento, liquidação contábil, preservação de período e idempotência sem nenhum reparo manual no Firestore. Contudo, o scratch script de homologação montou manualmente a fixture copiando valores arbitrários de testes unitários (`source 5/50` -> `target 15/200`). O Scenario A2 NÃO é a prova canônica das quotas do catálogo de produtos. Root cause: testes unitários isolados utilizavam 5/50 e 15/200, e o harness de teste copiou esses mocks em vez de chamar as factories de domínio de produção. Regra de homologação estabelecida: *SANDBOX HOMOLOGATION FIXTURES MUST NOT COPY UNIT-TEST SNAPSHOTS; ALWAYS USE PRODUCTION DOMAIN AUTHORITIES*. Não se trata de bug em código de produção: `PLANS_CATALOG` sempre foi 20/100 e 40/200, e o `SubscriptionService` aplicou fielmente o snapshot persistido.
      - *Scenario A1 Slot Contamination Disclosure*: O Scenario A1 executado no ciclo inicial verificou provider, hosted payment, settlement e promoção de entitlement, mas foi contaminado por intervenção manual no Firestore devido a formato de slot incorreto gerado exclusivamente pelo scratch script de teste (separador simples `_` vs canônico duplo `__`). O Scenario A1 NÃO é a prova E2E definitiva.
      - *Slot Harness Lesson (Homologation Note)*: Scripts de teste e fixtures de homologação DEVEM usar obrigatoriamente a função canônica `buildActiveTransitionSlotId(ministryId, provider)` (`slot_${safeMinistry}__${provider}`) e NUNCA duplicar o formato de slot ID manualmente. Auditoria de código confirmou que todo o runtime de produção em `BillingRepository` utiliza a autoridade canônica uniformemente.
      - *Snapshot Authority Chain*: Cadeia canônica provada de ponta a ponta no Scenario A3: `PLANS_CATALOG (Lite 20/100 -> Essential 40/200)` -> `buildTransitionCommercialSnapshot` -> `target_entitlement_snapshot (40/200)` -> `PAYMENT_CONFIRMED` -> `processEarlyActivationAdjustmentSettlement` -> `applyLockedEntitlementSnapshot` -> `ministry_subscriptions (locked_member_quota = 40, locked_song_quota = 200)` -> `SubscriptionService.getSubscriptionSummary` -> `Essential (40/200)`.
      - *Scenario B (Natural Expiry & Post-Expiry Safe Retry — Homologated Evidence Retained)*: Executado contra o Asaas Sandbox oficial com TTL mínimo de 10 minutos (`minutesToExpire = 10`). Ao atingir o TTL local (`now >= expires_at`), a rotina canônica avaliou o checkout como `local_expiry_awaiting_provider_webhook`, com zero chamadas de `POST /v3/checkouts/{id}/cancel` pós-TTL e slot retido em `HELD`. A expiração natural oficial do gateway foi comprovada pelo recebimento assíncrono do webhook oficial `CHECKOUT_EXPIRED` do Asaas. Gate de verificação financeira confirmou zero pagamentos na sessão (`listPaymentsByCheckoutSession = 0`). A tentativa transitou deterministicamente para `status = 'expired'`, `provider_session_terminal = true`, `expiry_confirmed_at` preenchido e campos de cancelamento explícito deixados como undefined. Zero `BillingTransaction` gerada. A transição permaneceu `scheduled` e o slot ativo permaneceu `HELD`. Nova cotação emitida com sucesso após a expiração segura com novo quote ID, pró-rata recalculada e histórico de auditoria intacto.
      - *Post-Test Sandbox Hygiene Correction & Disclosure*: No ciclo inicial, a rotina de limpeza pós-teste utilizou incorretamente `DELETE /v3/subscriptions/{id}` sobre assinaturas Sandbox descartáveis de teste. Auditoria do contrato do Asaas confirmou que `DELETE` remove a assinatura e suas faturas pendentes, enquanto a inativação não destrutiva canônica do provedor é `PUT /v3/subscriptions/{id}` com payload `{ status: 'INACTIVE' }` (já implementada em produção via `AsaasBillingProvider.inactivateSubscription`). Para os Scenarios A2, A3 e futuras homologações, foi adotada a disciplina estrita de zero `DELETE`, utilizando exclusivamente a inativação canônica `INACTIVE`. Nenhuma alteração de dados de produção foi realizada.
      - *Homologation Limitations & Open Scenarios*:
        - `LOST WEBHOOK FULL SANDBOX SIMULATION: NOT_VERIFIED`: A recuperação de checkouts conhecidos via reconciliador assíncrono foi validada no Sandbox (`known checkout -> listPaymentsByCheckoutSession -> settled payment correlation -> reconciler idempotency`), mas a supressão física intencional de entrega de webhook do gateway não foi simulada diretamente no ambiente Sandbox.
  - Phase 3D (Scheduled Cancel-to-Free / Downgrade to Free):
    - Phase 3D.0 (Audit & Design Gate): COMPLETE (Mapeamento de contratos, domínio e precedência V1).
    - Phase 3D.0B (Asaas Provider Contract Sandbox Proof): COMPLETE & SANDBOX PROVEN (Comprovado empiricamente no Sandbox: PUT status INACTIVE cessa renovações sem gerar cobranças; cobrança PENDING futura gerada antes da inativação sobrevive como pendente e exige remoção cirúrgica; DELETE cobrança pending exata é seguro e não afeta pagamentos quitados; idempotência de inativação e remoção comprovadas).
    - Phase 3D.1 (V1 Domain, Persistence & Internal Request Preparation): COMPLETE (Criação determinística de transição com estratégia `scheduled_cancel_to_free` e status inicial `awaiting_old_inactivation`; snapshot Free travado canonicamente em 10 membros / 50 músicas / 0 add-ons com economia zero; slot ativo canônico alugado via `buildActiveTransitionSlotId`; concorrência CAS-safe e idempotência de requisição duplicada; preservação estrita de direitos de uso e quotas pagas vigentes antes da fronteira comercial; guarda de conflito contra reativação legada via 409 `ACTIVE_CANCELLATION_TRANSITION_EXISTS` com zero mutações no gateway; 33 testes dedicados e 933 testes no backend passando).
    - Phase 3D.2 (Strict Provider Do-Not-Renew & Exhaustive Future Obligation Cleanup Hardening): COMPLETE & CHECKPOINTED (Orquestração interna de provedor para inativação estrita da assinatura de origem e limpeza cirúrgica de cobranças futuras PENDING implementada; contrato estrito de provedor `getSubscriptionState`, `inactivateSubscriptionStrict` e `listAllSubscriptionPaymentsStrict` com resultado estruturado fail-closed; 404 preservado como divergência de recurso [atenção financeira], nunca sucesso silencioso; enumeração paginada exaustiva e deduplicação de todas as cobranças da exata assinatura de origem com salvaguarda contra loops e conflitos de status; re-leitura fresh multi-página comprovando zero obrigações futuras antes da promoção para `scheduled`; preservação de histórico pago anterior; worker reconcilia `awaiting_old_inactivation` e trata `scheduled` como NO-OP explícito aguardando 3D.3; migração da rota pública `/billing/cancel` intencionalmente diferida para a Phase 3D.3 para garantir deployability e prevenir retenção indefinida de slot antes da implementação do boundary cutover; 56 testes dedicados na suíte 3D.2 e 1.013 testes no backend passando).
    - Phase 3D.3 / 3D.3A (Period-End Boundary Cutover to Free, Entitlement Convergence, Terminal Atomicity & Live Ownership Lifecycle Hardening): COMPLETE & CHECKPOINTED:
      - *Entitlement Authority & Anti-Bypass Guard*: `active_cancellation_transition_id` atua como projeção viva de ownership V1 em `ministry_subscriptions`. Enquanto ativo (`awaiting_old_inactivation`, `scheduled`, `financial_attention_required`), impede categoricamente que o clock-based fallback legado de `cancel_at_period_end` converta a assinatura em Free durante erros transitórios de rede ou pendências financeiras. Cancelamentos históricos legados (sem o marker) preservam seu comportamento sem regressão.
      - *Marker Lifetime Invariant*: O marker é gravado quando a transição V1 se torna autoritativa; retido em todos os estados não terminais e durante o crash após a aplicação do snapshot Free (STEP 4); e limpo estritamente na terminalização segura atômica via CAS compare-and-clear. Não é referência histórica; o histórico contábil permanece nos registros `BillingTransition`.
      - *Canonical Slot Identity & Single Authority*: O slot determinístico utiliza exclusivamente o helper `buildActiveTransitionSlotId(ministryId, provider)` (`slot_${ministryId}__${provider}`) e a coleção `billing_active_transition_slots`. Zero concatenações manuais de string.
      - *Atomic Terminalization (Eliminação de Crash Window C)*: `BillingRepository.completeTransitionAndReleaseOwnedSlotAtomically` executa em uma única transação Firestore: (1) atualiza a transição para `completed` + `safe_terminal`; (2) deleta o slot ativo determinístico (`t.delete`); e (3) limpa o ownership marker (`active_cancellation_transition_id: null`, `cancel_at_period_end: false`) via CAS compare-and-clear. Pré-condições estritas: transição não pode estar em atenção financeira (`financial_attention_required !== true && financial_safety_status !== 'attention_required'`), assinatura do ministério deve existir, slot deve pertencer exclusivamente à transição, e marker na assinatura deve corresponder ao ID da transição. Impossibilita o surgimento do estado órfão `completed` + `HELD`.
      - *Future-Safe Locked Target Snapshot*: O boundary reconciler valida invariantes de domínio (`plan_id === 'free'`, `addon_blocks === 0`, cotas numéricas `>= 0`) sem hardcodar valores estáticos do catálogo (10/50), garantindo que o snapshot congelado no momento da solicitação seja a autoridade soberana e imune a evoluções legítimas de catálogo futuro.
    - Phase 3D.3B / 3D.3B.1 (Strict Cancel-to-Free Marker CAS & Strategy Isolation — Final Pre-Sandbox Hardening): COMPLETE:
      - *Strategy Gate (Phase 3D.3B.1)*: `completeTransitionAndReleaseOwnedSlotAtomically` é exclusivo de `scheduled_cancel_to_free`. A pré-condição 0 rejeita qualquer outra `execution_strategy` V1 (`scheduled_paid_transition`, `immediate_initial_purchase`) com `unsupported_transition_strategy` antes de qualquer mutação — defense-in-depth no repositório, independente dos guards no service. Zero efeitos colaterais sobre slot, marker ou transition ao rejeitar por strategy.
      - *Invariante de CAS escopo-restrito (Phase 3D.3B.1 corrige 3D.3B)*: `active_cancellation_transition_id` é invariante EXCLUSIVO de `scheduled_cancel_to_free`. **NÃO** é invariante global de V1. `financial_safety_status === 'live'` NÃO determina a obrigatoriedade do marker — apenas `execution_strategy === 'scheduled_cancel_to_free'` combinado com `transition_status ∈ {scheduled, awaiting_old_inactivation}` (estados ativos de cancellation). Outras V1 vivas não herdam semânticas de cancellation marker.
      - *Marker ausente/divergente em estados ativos*: Para `scheduled_cancel_to_free` com `transition_status === 'scheduled'` ou `transition_status === 'awaiting_old_inactivation'`, `active_cancellation_transition_id` ausente (null/undefined/'') ou divergente → FAIL CLOSED com razão `subscription_marker_missing_or_divergent`.
      - *Callsite audit*: 2 callsites de produção em `billing.service.ts:2725` e `billing.service.ts:3170`, ambos dentro de `reconcileScheduledCancelToFreeBoundary` que já tem guard de estratégia na linha 2716. Nenhum outro callsite. Nenhuma outra estratégia pode atingir o helper por código de produção.
      - *Cobertura*: 18 testes no describe block: tests 1-10 (pré-condições canônicas), tests 11-15 (marker ausente/divergente), test 16 (regressão status não-activo), tests 17-18 (strategy isolation: paid_transition e initial_purchase → unsupported; zero mutações). 1.114 testes no backend passando. 48 testes no web passando.
    - Phase 3D.3B.2 (Strict Terminal Source Status — Final Pre-Sandbox State-Machine Hardening): COMPLETE:
      - *Strict Source Status Gate*: `completeTransitionAndReleaseOwnedSlotAtomically` aceita como caminho normal de terminalização EXCLUSIVAMENTE transições com `execution_strategy === 'scheduled_cancel_to_free'` AND `transition_status === 'scheduled'` AND `financial_safety_status === 'live'` AND `financial_attention_required !== true`. Qualquer outro status de origem é sumariamente rejeitado com `invalid_terminalization_source_status` antes de qualquer mutação.
      - *Non-Terminal & Terminal Invariants*: `awaiting_old_inactivation` não pode terminalizar pelo helper (safety de DO-NOT-RENEW ainda em trânsito); estados terminais históricos (`failed`, `canceled`, `superseded`) são imutáveis e não podem ser reescritos para `completed`; atenção financeira (`attention_required` ou `financial_attention_required === true`) é bloqueada por pré-condição fail-closed (`financial_attention_required`).
      - *Idempotency & Repair Scope*: O único outro estado aceito pelo helper é `transition_status === 'completed'` AND `financial_safety_status === 'safe_terminal'`, restrito estritamente a idempotência e reparo CAS-safe de slot still-HELD e/ou marker residual. `completed` com `financial_safety_status !== 'safe_terminal'` (ex: `completed/live`) falha fechado com `invalid_terminalization_source_status`.
      - *Callsite & Invariant Verification*: Os dois callsites de produção em `billing.service.ts` (`L2725`, `L3170`) respeitam estritamente essa invariante: L2725 checa `safe_terminal` antes da chamada, e L2747 descarta status divergentes de `scheduled` antes da fronteira comercial em L3170.
      - *Cobertura*: 24 testes no describe block de terminalização atômica (testes 14 e 16 corrigidos para fail-closed; novos testes 19 a 24 cobrindo failed, canceled, completed/live, completed/attention, idempotência limpa e invariante de zero-mutação). 1.120 testes no backend passando. 48 testes no web passando.
    - Phase 3D.3B.3 (Strict Financial Safety Source Gate — Final Pre-Sandbox Invariant): COMPLETE:
      - *Strict Scheduled + Live Source Gate*: `completeTransitionAndReleaseOwnedSlotAtomically` exige cumulativa e explicitamente `transition.transition_status === 'scheduled' && transition.financial_safety_status === 'live'`.
      - *Crossed States Fail Closed*: `scheduled + safe_terminal` é estado inconsistente e agora falha fechado com `invalid_terminalization_source_status` sem mutação de transição, slot nem marker. `completed + live` e `completed + attention` falham fechado. `completed + safe_terminal` permanece o único par terminal idempotente aceito.
      - *Cobertura*: 26 testes no describe block de terminalização atômica (testes 25 e 26 adicionados cobrindo scheduled/safe_terminal e invariante de zero-mutação). 1.122 testes no backend passando. 48 testes no web passando.
    - Phase 3D.4 (Scheduled Cancel-to-Free End-to-End Sandbox Homologation): IN PROGRESS / SANDBOX CONTRACT DIVERGENCE IDENTIFIED:
      - *Sandbox Orchestration Verified (Scenario A)*: Proved authoritative public V1 entrypoint against Asaas Sandbox (`3D4_1788551805209_3T367`): customer isolado criado, source recurrence criada como `ACTIVE`, obrigação futura `PENDING` materializada no gateway. Entrypoint público `BillingService.cancelSubscription` acionado: reivindicou slot canônico determinístico `slot_${ministryId}__${provider}` (`HELD`), setou `active_cancellation_transition_id` e `cancel_at_period_end: true`, colocou a assinatura no Asaas em `INACTIVE` (sem `DELETE`), removeu cirurgicamente a cobrança futura `PENDING` no gateway (0 cobranças bloqueantes comprovadas via fresh read), congelou snapshot Free (10/50) em transição `scheduled` + `live`.
      - *Pre-Boundary Invariant Proven*: Durante `now < effective_at`, `SubscriptionService.getSubscriptionSummary` preservou estritamente a titularidade paga Essential (40 membros / 200 músicas). Reconciler worker executado pré-fronteira respeitou o tempo, operando como NO-OP seguro. Chamada duplicada a `cancelSubscription` provou idempotência limpa.
      - *Sandbox Contract Divergence Identified (Boundary Reconciler Claim Lock)*: Ao atingir a fronteira temporal real (`now >= effective_at`), a reconciliação pelo worker em `reconcileScheduledCancelToFreeBoundary` invocava `BillingRepository.claimPlanChangeForRetry`. Esse método possui guarda legada (linha 3168) que rejeita claims onde `supersede_status === 'completed'`. Como o passo anterior de `do-not-renew` havia gravado `supersede_status: 'completed'` junto de `transition_status: 'scheduled'`, o claim retornou nulo (`locked_by_another_worker`), impedindo o avanço para `completed` / `safe_terminal` e o cutover para Free. O sistema falhou fechado com segurança (titularidade paga permaneceu intacta). **CORRIGIDO em Phase 3D.4A** (ver abaixo).
    - Phase 3D.4A (V1 Reconciliation Claim Authority Hardening): COMPLETE:
      - *Root Cause*: `reconcileScheduledCancelToFreeBoundary` e `reconcileScheduledCancelToFreeDoNotRenew` usavam `claimPlanChangeForRetry` (primitive legada) em vez de `claimTransitionForReconciliation` (primitive V1). A primitive legada tem guarda `supersede_status === 'completed' → return null`. O `do-not-renew` escreve `supersede_status: 'completed'` ao promover `awaiting_old_inactivation → scheduled`. O `boundary` subsequente recebia `null` da claim e retornava `locked_by_another_worker`.
      - *Fix Aplicado*: Ambas as funções agora usam `claimTransitionForReconciliation`. Autoridade V1: terminality governada exclusivamente por `transition_status === 'completed' AND financial_safety_status === 'safe_terminal'`. Campo `supersede_status` NÃO é e NUNCA deve ser critério de terminality para V1. Guards `isBillingTransitionV1` removidos (tornaram-se `never` branches: V1 claim já garante o tipo).
      - *Legacy Preserved*: `claimPlanChangeForRetry` permanece inalterada. `processPlanChangeSupersede` (legacy) e `cutoverPaidToPaidSourceRecurrence` (V1 paid-to-paid, transition status determina próxima chamada via worker branch) continuam usando a primitive correta para cada contexto.
      - *Claim/Release Pairing*: Ambas as primitives usam os mesmos lease fields (`retry_locked_until`, `retry_locked_by`) e são liberadas via `releasePlanChangeLock`. Pairing inalterado.
      - *Cobertura*: +5 testes de regressão Sandbox (R1–R5) em `billing-scheduled-cancel-to-free-boundary.test.ts`: R1 prova que scheduled/live + supersede_status completed é claimável via V1; R2 prova no-premature-cutover; R3 prova que completed/safe_terminal ainda é excluído; R4 documenta root cause (legacy guard proof); R5 prova que do-not-renew também usa V1 claim. 1.127 testes no backend passando. 48 testes no web passando.
    - Phase 3D.4R (Scheduled Cancel-to-Free Sandbox Repeat — Authoritative Re-Run): COMPLETE & CLEAN END-TO-END SANDBOX HOMOLOGATED (Homologação autoritativa concluída contra o Asaas Sandbox oficial em 2026-09-07; 0 alterações em backend/src/** ou web/src/**; ambos os cenários A e B aprovados com evidência autoritativa completa):
      - *Scenario A (Authoritative Complete End-to-End Cancel-to-Free Cutover)*: Prova autoritativa definitiva com fixtures isoladas (`3D4R_1788809315473_n4mm1`). Customer Asaas isolado criado (`cus_...3929`) com assinatura recorrente ativa (`sub_...p9yy`, Essential R$ 34,90/mês) e cobrança futura PENDING materializada (`pay_...4y5v`). Baseline Essential 40/200 ativo com slot ABSENT. Entrypoint público V1 `BillingService.cancelSubscription` acionado: reivindicou slot canônico determinístico `slot_${ministryId}__${provider}` (HELD), persistiu marker `active_cancellation_transition_id` e `cancel_at_period_end: true`, colocou a assinatura no Asaas em `INACTIVE` (sem `DELETE`), e removeu cirurgicamente a obrigação futura PENDING (0 pagamentos bloqueantes via fresh read). Snapshot Free (10/50) travado em transição `scheduled` + `live`. Pré-fronteira: titularidade paga Essential 40/200 estritamente preservada; ciclo de reconciliação pré-fronteira operou como NO-OP seguro mantendo transição `scheduled` e slot `HELD`; chamada duplicada comprovou idempotência. Fronteira temporal atingida via relógio acelerado (56s): estado anterior de falha rigorosamente comprovado (`supersede_status: 'completed'`, `transition_status: 'scheduled'`, `financial_safety_status: 'live'`, slot HELD, marker set). Ciclo do `BillingReconcilerWorker` executado na fronteira: lock V1 adquirido com sucesso via `claimTransitionForReconciliation` (provando o fix da Phase 3D.4A); cutover atômico completado. Terminalização atômica em transação Firestore única comprovada: `transition_status: 'completed'` + `financial_safety_status: 'safe_terminal'`, slot ativo determinístico deletado (`ABSENT`), marker limpo (`null`), `cancel_at_period_end: false`, billing subscription atualizada para `free` e status `canceled`. Entitlement LouvAIO convergiu para Free (10 membros, 50 músicas). Recurso no Asaas permaneceu preservado como `INACTIVE` (ZERO `DELETE /subscriptions/{id}`). Ledger financeiro 100% limpo (0 transações contábeis criadas). Idempotência pós-convergência comprovada (worker NO-OP e chamada posterior a `cancelSubscription` rejeitada com HTTP 400 informando ausência de plano ativo).
      - *Scenario B (Authoritative External Provider Reactivation Divergence Proof)*: Prova autoritativa de fail-closed com fixtures isoladas (`3D4R_B_1788809398185_k1g6m`). Customer Asaas isolado criado (`cus_...3936`) com assinatura recorrente ativa (`sub_...ddxz`). Cancelamento V1 executado até transição `scheduled` + `live` com `supersede_status: completed`. Simulação de divergência externa do provedor: assinatura de origem reativada diretamente no Asaas para `ACTIVE` via `reactivateSubscription`. Fronteira atingida (56s) e ciclo do reconciliador executado contra a fixture divergente. Boundary guard acionado: detectou assinatura inesperadamente `ACTIVE` no provedor (`source_subscription_reactivated`). Fail-closed rigorosamente comprovado: `financial_attention_required: true`, `financial_safety_status: 'attention_required'`, `financial_attention_reason: 'source_subscription_reactivated'`, slot canônico determinístico mantido em `HELD`, marker de cancelamento mantido intacto em `ministry_subscriptions`, e titularidade paga Essential 40/200 estritamente preservada no runtime (zero downgrade prematuro, zero perda de quotas). Higiene conservadora comprovada pós-teste: assinatura de origem inativada com sucesso (`INACTIVE`, ZERO `DELETE`).
      - *Phase 3D Status*: Integralmente homologada no Asaas Sandbox oficial e CONCLUÍDA.
  - Phase 4A (Customer-Facing Billing Surface & Pending Transitions):
    - Phase 4A.0 (Billing Product Surface Inventory): COMPLETE & READ-ONLY (Gap analysis comprovou necessidade de projeção customer-facing de pending transitions e delinquency sem reabertura do core financeiro).
    - Phase 4A.1 (Customer Billing Summary Normalization — Pending Transition + Delinquency Contract): COMPLETE (Contrato `MinistrySubscriptionStatusSummary` normalizado com `pendingTransition`, `paymentStatus` e `graceReason`; DTO customer-safe `CustomerFacingPendingTransitionDto` isola estados internos de V1 e preserva imutabilidade dos snapshots travados; resolução via slot determinístico ativo sem scans; estados terminais `completed + safe_terminal`, `canceled`, `superseded` e `failed` estritamente excluídos de pending; discriminação segura entre carência por inadimplência [`payment_failure`] e excesso de uso [`usage_over_limit`]; modos cortesia e free protegidos contra fabricação de inadimplência; isolamento de tenant estrito com zero vazamento de identificadores de provedor ou locks internos; zero mutações no core financeiro da Phase 3; 21 testes unitários dedicados do mapper e 4 testes de integração no SubscriptionService; 1.152 testes no backend e 48 testes no web passando).
    - Phase 4A.2 (Current Plan + Scheduled Transition UX — Customer-Facing Billing State + Checkout Return Correction): COMPLETE (Projeção do resumo customer-facing normalizado da Phase 4A.1 na interface web em `SubscriptionPlanView.tsx`; Card dedicado de transição agendada/pendente exibindo kind legível em pt-BR, status, snapshots de origem e destino, vigência comercial e preservação absoluta de dados em cancel_to_free; Indicador de ciclo de cobrança vigente [Mensal / Anual] no card do plano atual; Correção da convergência no polling pós-checkout eliminando falsos timeouts de 45s para transições agendadas via matching de pendingTransition; Tratamento seguro de retornos de checkout com cancelamento explícito [`status=cancel`] e link expirado [`status=expired`] com limpeza de intent, descarte de polling, mensagens claras ao usuário e consumo limpo de query params; Proteção contra intents obsoletas e isolamento estrito de tenant; Zero mutações no core financeiro da Phase 3; 22 novos testes cobrindo integralmente as matrizes Section 41 e Section 42; 70 testes no web e 1.152 testes no backend passando).
    - Phase 4A.3 (Addon Management + Non-Admin Read-Only UX — Current-Plan Addon Changes + Frontend RBAC Alignment): COMPLETE (Suporte a gestão de blocos de membros adicionais no plano pago ativo [Essential / Pro] diretamente no card do plano atual via controle dedicado "Gerenciar adicionais" e modal com stepper touch-friendly [min 44px] delimitado em [0, maxMemberAddonBlocks]; Integração estrita com backend autoritativo via GET /billing/preview para precificação, cotas de membros e verificação de capacidade [downgradeImpact / isOverLimit / gracePeriod]; Reutilização da máquina de checkout V1 sem modificações no backend, classificando addon_change como scheduled_paid_transition e gravando CheckoutIntent com expectedAddonBlocks; Invariante de slot ativo respeitada [botão desabilitado com tooltip "Já existe uma alteração de assinatura em andamento." quando há pendingTransition]; Prevenção de mutações no-op [botão desabilitado quando alvo == atual]; Alinhamento rigoroso com RBAC do backend [canManageBilling={userRole === 'admin'}]: membros não-admin operam em modo somente leitura seguro, com botões de mutação no catálogo desabilitados ["Apenas administradores"], controles de adicionais bloqueados, ausência de botões de cancelamento ou alteração de adicionais, e banner explicativo sem ações que gerem HTTP 403; Isolamento de tenant e ministério estrito [fechamento automático de modais abertos e reversão a read-only ao alternar ministérios]; ZERO alterações em backend/src/**; 20 novos testes dedicados cobrindo integralmente as matrizes Section 35 [Read-Only UX], Section 36 [Addon Management] e Section 37 [Tenant & Role Isolation]; 90 testes no web e 1.152 testes no backend passando).
    - Phase 4A.4.0B (Asaas Cancellation Reversal Sandbox Probe — Future Payment Materialization Contract): COMPLETE & SANDBOX PROVEN (Sonda empírica autoritativa concluída contra o Asaas Sandbox oficial api-sandbox.asaas.com em 2026-09-07 com fixture isolada 4A4_0B_1788831462654_4v0b8: customer cus_...5687 e assinatura sub_...0qym; baseline comprovado com 1 cobrança PENDING inicial pay_...3nwy; simulação de V1 do-not-renew concluída com inativação via PUT status INACTIVE e exclusão cirúrgica de pay_...3nwy; pré-reativação comprovada com 0 cobranças futuras; reativação da exata assinatura de origem via PUT status ACTIVE com nextDueDate 2026-09-09 concluída com sucesso HTTP 200, retendo o mesmo ID e status ACTIVE; janela de observação temporal de 120s [T+0s a T+120s] comprovou 0 cobranças geradas [classificação NOT_MATERIALIZED_WITHIN_PROBE_WINDOW], provando que o Asaas NÃO materializa cobrança PENDING síncrona nem em curto prazo assíncrono após reativação de assinatura cuja cobrança futura foi excluída; prova de idempotência com segundo PUT ACTIVE comprovou 0 cobranças duplicadas e estabilidade do status ACTIVE; higiene final inativou a assinatura sem nenhum DELETE de subscription e zero estornos; implicação direta de design para V1 reversal: a presença imediata de cobrança PENDING não pode ser pré-requisito de terminalização do uncancel, devendo a autoridade de restauração da recorrência ser governada pelo contrato da assinatura [status ACTIVE + nextDueDate exato] acompanhado de ausência comprovada de obrigações conflitantes; zero alterações em backend/src/** ou web/src/**).
    - Phase 4A.4.1 (V1 Cancellation Reversal Domain + Persistence Foundation): COMPLETE (Fundação de domínio e persistência para reversão durável de cancelamento [uncancel] antes da fronteira do período implementada; Tipo `BillingCancellationReversalStatus = 'requested' | 'completed' | 'attention_required'` e campos de subworkflow write-once em `BillingTransitionV1Record`; Validação em runtime estrita em `validateBillingTransitionV1` assegurando backward compatibility e isolamento exclusivo para `scheduled_cancel_to_free`; Primitivas atômicas de persistência em `BillingRepository`: `beginCancellationReversalAtomically` [CAS estrito de boundary `now < effective_at`, checks de slot/markers, concorrência segura, idempotência com timestamps preservados, retenção de slot HELD e markers de ownership], `completeCancellationReversalAndReleaseOwnedSlotAtomically` [transação Firestore de 4 documentos com detecção fail-closed de divergência terminal, compare-and-clear atômico do ownership marker `active_cancellation_transition_id: null`, deleção do slot ativo determinístico, reversão de `cancel_at_period_end: false` em `ministry_subscriptions` e `billing_subscriptions`, transição para `transition_status = 'canceled'` + `financial_safety_status = 'safe_terminal'` e preservação 100% intacta de planos/quotas/entitlements], e `setCancellationReversalAttentionAtomically` [marcação de atenção financeira sem liberação prematura de slot]; Visibilidade canônica em `getV1TransitionsNeedingReconciliation` comprovada; 59 novos testes dedicados no repositório e mapper; 1.225 testes no backend e 90 testes no web passando; ZERO alterações no gateway Asaas ou UI).
    - Phase 4A.4.1A (Cancellation Reversal Foundation Hardening — Mandatory Lease + Provider Binding + Attention Scope + Subworkflow Consistency): COMPLETE (Remediação integral dos 5 achados F1-F5 da revisão independente 4A.4.1-R: F1 - `options.expectedLockOwner` tornado obrigatório em `completeCancellationReversalAndReleaseOwnedSlotAtomically` com CAS estrito de lease [`lease_required`, `lease_not_owned`, `lease_expired`] no caminho normal; F2 - `options.expectedLockOwner` tornado obrigatório em `beginCancellationReversalAtomically` exigindo lease unexpired válido e validação de `actorUserId` não-vazio [`actor_user_id_required`]; F3 - validação explícita de `transition.provider === provider` adicionada em `beginCancellationReversalAtomically`, `completeCancellationReversalAndReleaseOwnedSlotAtomically` e `setCancellationReversalAttentionAtomically` falhando fechado com `provider_mismatch`; F4 - restrição estrita de escopo em `setCancellationReversalAttentionAtomically` exigindo `transition_status === 'scheduled'` e `financial_safety_status === 'live'` [`invalid_source_status`], `cancellation_reversal_status === 'requested'` [`reversal_not_requested`], tenant e provider matching, lease válido, e verificação de posse de slot determinístico e marker de assinatura retendo slot HELD e cotas intactas; F5 - validação consistente em `validateBillingTransitionV1` exigindo `requested_at` e `requested_by` para qualquer status de reversão, rejeição de `completed_at`/`attention_reason` em 'requested', rejeição de `attention_reason` em 'completed', rejeição de `completed_at` em 'attention_required', e rejeição total de campos órfãos; 17 novos testes dedicados cobrindo F1-F5; 1.242 testes no backend e 90 testes no web passando; zero alterações no Asaas ou web).
    - Phase 4A.4.2 (V1 Cancellation Reversal Provider Orchestration — Strict Asaas Reactivation + Crash Recovery + Reconciler Safety): COMPLETE (Orquestração de provedor e recuperação de falhas para reversão de cancelamento agendado implementada; Método `reactivateSubscriptionStrict` adicionado à interface `BillingProvider` e implementado no `AsaasBillingProvider` com PUT /v3/subscriptions/{id} enviando status ACTIVE e nextDueDate, classificação estrita de erros [SUCCESS, NOT_FOUND, AUTH_ERROR, CLIENT_ERROR, TRANSIENT_ERROR, MALFORMED_RESPONSE] e parseamento do estado retornado; Extensão do tipo de domínio com status `'expired'` para expiração limpa de reversão na fronteira periódica com validação de runtime; 5 attention reasons canônicas adicionadas em `CANCELLATION_REVERSAL_ATTENTION_REASONS`; Orquestração em 5 etapas em `BillingService` cobrindo Crash Window A [reativação prévia sem avanço local detectada no GET inicial], Crash Window B [falha transitória no PUT recuperada por fresh GET confirmando ACTIVE], Crash Window C [falha de rede transitória na listagem de pagamentos retendo lease para retry assíncrono], validação rigorosa de segurança de pagamentos com tolerância zero para pagamentos liquidados inesperados ou duplicatas e fail-closed com atenção sem exclusão de cobranças; Second-gate proof exigindo fresh read ACTIVE no gateway antes de terminalizar; Idempotência total e ordenação correta de status; Roteamento no `BillingReconcilerWorker` direcionando `scheduled_cancel_to_free` com `requested` para `reconcileScheduledCancellationReversal`; 38 novos testes unitários e de integração cobrindo 100% dos fluxos de sucesso, erro, janelas de queda, fronteira periódica e idempotência; 1.301 testes no backend e 90 testes no web passando; ZERO alterações em web/src/** e ZERO mutações no Asaas Sandbox).
    - Phase 4A.4.2A (Distinguish New Reversal Request from Crash Recovery — External Active Divergence + No-Active-Cancellation Semantics): COMPLETE (Remediação integral dos findings F1 [HIGH] e F2 [MEDIUM] da revisão 4A.4.2-R; Distinção canônica de proveniência de invocação via `reversalExecutionMode: 'new_request' | 'recovery'`: `requestScheduledCancellationReversal` deriva o modo diretamente do retorno atômico de `beginCancellationReversalAtomically` [`beginRes.reason === 'reversal_already_requested' ? 'recovery' : 'new_request'`], enquanto `reconcileScheduledCancellationReversal` opera estritamente como `'recovery'`; Em `new_request`, constatar status `ACTIVE` no provedor antes de qualquer emissão de PUT pelo LouvAIO é tratado como divergência de reativação externa, falhando fechado com atenção financeira [`source_subscription_reactivated`], retendo o slot HELD, marker e cancel flags, sem mutações no gateway e liberando o lease; Somente transições com proveniência durável prévia de `'requested'` qualificam-se para Crash Window A recovery com tolerância zero a PUTs duplicados; Eliminação do fallback incorreto para `reversal_already_completed` em assinaturas ativas sem cancelamento agendado, retornando estritamente `no_active_cancellation_found` na ausência de cancelamento ativo canônico, e preservando idempotência terminal apenas quando chamado com `transitionId` explícito de reversão completada; 41 testes de orquestração passando [100% de cobertura dos caminhos 5.1A/5.1B, 2.2b e 3.2]; 1.304 testes no backend e 90 testes no web passando; ZERO alterações em web/src/** ou Asaas).
    - Phase 4A.4.3 (Public V1 Cancellation Reversal API + Web Uncancel UX — Connect Canonical Reversal Engine to Customer Flow): COMPLETE (Conexão do endpoint `POST /api/v1/ministries/:ministryId/billing/reactivate` ao motor canônico V1 `requestScheduledCancellationReversal` quando existe transição `scheduled_cancel_to_free` ativa, preservando 100% de compatibilidade com reativação legada na ausência de cancelamento V1; Autenticação e RBAC rigorosos via token com `req.user.id` obrigatório como `actorUserId`; Chamadas não-autenticadas/sem ator preservam a guarda 409 da Phase 3D; Mapeamento customer-safe de erros HTTP no backend [200 sucesso / idempotência, 400 sem cancelamento ativo / prazo expirado, 409 concorrência / atenção financeira, 503 falha transitória do provedor], sem vazamento de dados de infraestrutura ou stack traces; UI web em `SubscriptionPlanView.tsx` expõe CTA "Desfazer cancelamento" em destaque no card de transição pendente com visibilidade condicionada estritamente a `kind === 'cancel_to_free'`, `status !== 'attention_required'` e `canManageBilling === true`; Modal acessível de confirmação com suporte a Escape/backdrop, proteção contra duplo clique, estado de loading com spinner, toasts amigáveis e re-leitura autoritativa via `loadData()` que oculta o card pendente; Segurança contra tenant-switch [`currentMinistryIdRef`] e rebaixamento de papel fechando modais imediatamente; Fix do achado remanescente LOW da fase 4A.4.2-R2 com asserção explícita de `releasePlanChangeLock` no teste 5.4; 14 testes de integração da API pública no backend, 2 no controller, e 13 novos testes web cobrindo as Seções 36-43; 1.320 testes backend e 103 testes web passando; ZERO mutações em providers/** ou Asaas Sandbox).
    - Phase 4A.4.3A (Public Uncancel Integration Hardening — Fail-Closed Slot/Marker Divergence + Response Minimization + Refresh UX): COMPLETE (Remediação dos 3 findings da revisão independente 4A.4-R: F1 [HIGH] - Detecção de divergência de integridade de posse em `BillingService.reactivateSubscription`: quando o slot determinístico V1 está ausente [`activeSlot == null`], mas a assinatura da aplicação ainda retém o marcador `active_cancellation_transition_id`, o sistema falha fechado com HTTP 409 `FINANCIAL_ATTENTION_REQUIRED` [`slot_missing_with_active_cancellation_marker`] com ZERO mutações no gateway provedor [`reactivateSubscription` 0 chamadas, `reactivateSubscriptionStrict` 0 chamadas], retendo marcadores de cancelamento e integridade dos planos intactos; F2 [MEDIUM] - Minimização e sanitização rigorosa da resposta pública no controller: `BillingController.reactivateSubscription` retorna estritamente DTO sanitizado `{ success: true, message: string, outcome: 'cancellation_reversed' | 'legacy_reactivated' }`, eliminando 100% de vazamento de identificadores internos e do provedor [`subscription`, `reversalResult`, `provider_subscription_id`, `payment_cleanup_ids`, `retry_locked_by`]; interface `BillingReactivationResponse` introduzida no cliente web em `api.ts`; F3 [LOW] - Desacoplamento de mutação e recarregamento autoritativo em `SubscriptionPlanView.tsx`: execução da mutação protegida em bloco isolado de forma que falha de rede no recarregamento subsequente via `loadData()` NUNCA mascara a reversão bem-sucedida como falha de mutação nem induz o usuário a novo clique com falso erro, exibindo aviso informativo para atualizar a página e retendo o sucesso da operação; testes de regressão no backend e web cobrindo F1, F2 e F3; 1.323 testes no backend e 105 testes no web passando; ZERO alterações em `providers/**`, `BillingRepository.ts` ou Asaas).
    - Phase 4A.5 (Customer Early Activation UX — Expose Existing V1 Prorated Early-Activation Engine Without Duplicating Financial Logic): COMPLETE (Exposição da ativação antecipada com ajuste proporcional desenvolvida na Phase 3C para o cliente final sem duplicar lógica financeira nem criar autoridade paralela no frontend; Backend autoritativo estrito para cotação, elegibilidade, ajuste proporcional em centavos, vinculação de checkout e liquidação; Normalização customer-facing em `customer-transition-summary.mapper.ts` mapeando `earlyActivation` com estados `'eligible' | 'not_applicable' | 'payment_pending' | 'activated' | 'expired' | 'boundary_reached'`, restringindo estritamente elegibilidade a `plan_upgrade` e `addon_increase` [downgrades, reduções de adicionais, cancel_to_free e attention_required são categoricamente mapeados para not_applicable]; Sanitização total sem vazamento de identificadores de provedor; DTOs e cliente web em `api.ts` com métodos `getEarlyActivationQuote` e `createEarlyActivationCheckout`; Interface em `SubscriptionPlanView.tsx` renderiza no card de transição pendente: CTA "Ativar agora" para admins quando elegível, mensagem de aguardando confirmação + link quando `payment_pending`, e badge de confirmação quando `activated`; Modal de prévia com cálculo autoritativo exibindo transição, dias restantes/totais, valor proporcional em BRL, próxima renovação e aviso explicativo; Proteção contra duplo clique e gravação de `CheckoutIntent` com `type: 'early_activation'`; Polling pós-checkout seguro com convergência comprovada via `status === 'activated'` ou planId, descarte limpo em expiração/cancelamento, e salvaguarda contra loops; Isolamento estrito de tenant [`currentMinistryIdRef`] abortando cotações e checkouts in-flight e rebaixamento de perfil fechando modais imediatamente; Zero concessão de direito de uso local no frontend; 10 novos testes unitários no mapper backend [1.333 testes no backend passando] e 22 novos testes dedicados no web [127 testes no web passando]; ZERO alterações em providers/**, máquinas de liquidação ou Asaas Sandbox).



## Known Issues and Implementation Gaps

Itens duráveis e priorizados catalogados em docs/system-status.md:
- Asaas Customer Reuse (GAP-011): CLOSED — SANDBOX REVALIDATED (1 Ministry + provider vincula-se a 1 customer canônico em `billing_customers` com reutilização estrita em checkouts subsequentes, lock atômico de criação, fallback por externalReference e preservação de customers históricos).
- Same-Plan Interval Change (GAP-012): UI / interval recognition validado em Sandbox; execução financeira segue a Política V1 de agendamento em `current_period_end` (APPROVED DOMAIN POLICY REVISED — IMPLEMENTATION PENDING).
- Aliases de rota legados (`features/groups`) e rota de cifra por música (`/smart-chords/song/:songId`).

## Current State

- Sistema completo de Planos, Quotas, Entitlements e Assinatura por Ministério implementado no backend e web.
- Integração de SaaS Billing com Asaas implementada no backend e web (fluxos de Initial Purchase 3A, Paid-to-Paid 3B, Early Activation 3C e Cancel-to-Free 3D homologados em ambiente Sandbox; gaps conhecidos permanecem abertos; deploy/configuração de produção não devem ser presumidos).
- O web possui rotas estáveis, navegação mobile e desktop coerente, bootstrap autenticado, tela `/ministerio/plano` com checkout e controle de ciclo anual/mensal, testes E2E com cobertura de viewports (light/dark) e PWA com manifest LouvAIO atualizado.
- O repositório contém `backend` e `web`; não contém mobile ou supabase.

## Completed Milestones

- Estrutura inicial backend/web e migração para Firebase.
- Gestão de ministérios, repertório, escalas e PWA presentes no código.
- Reestilização integral da identidade visual e PWA LouvAIO.
- Implementação do Sistema de Planos, Quotas e Entitlements por Ministério.
- Formalização da Estratégia de Precificação Comercial v1 e Benchmark LouveApp.
- Otimização Pré-Lançamento de Custos, Consultas e Escalabilidade.
- Hardening de Segurança em Autenticação, Autorização, RBAC e Anti-IDOR.
- Integração de SaaS Billing, Checkout Hospedado, Webhooks Idempotentes, Future Payment Cleanup, Worker de Reconciliação, Early Activation Proration/Settlement e Cancel-to-Free com Asaas (fluxos 3A, 3B, 3C e 3D homologados em Sandbox; gaps de customer reuse e troca de ciclo no mesmo plano permanecem catalogados).

## Current Work

Consulte docs/exec-plans/active/; planos concluídos ficam em docs/exec-plans/completed/.
