# System Status and Evidence Matrix

Snapshot do estado operacional e técnico do LouvAIO. Este documento separa fatos de lacunas e não autoriza correções incidentais.

## Classification Rules

- **Confirmed behavior**: fluxo diretamente sustentado pelo código/configuração atual.
- **Confirmed inconsistency**: dois trechos ativos possuem contratos, identificadores ou expectativas incompatíveis.
- **Incomplete implementation**: código ou configuração existe, mas não completa o fluxo declarado.
- **Outdated documentation**: afirmação documental contradita pelo checkout.
- **Unknown / Not yet verified**: não há evidência local suficiente.

## Confirmed Behavior

| Area | Behavior | Evidence |
| --- | --- | --- |
| Runtime | Há dois pacotes npm independentes: backend e web. | backend/package.json, web/package.json |
| Frontend | React Router sincroniza módulos, detalhes de músicas/pastas/escalas e subseções do ministério com URLs estáveis. | web/src/main.tsx, web/src/App.tsx, web/src/routing.ts |
| Plans & Quotas | Sistema de planos e quotas por ministério (Free, Lite, Lite+, Essential, Pro, Premium), com resolução funcional de accessMode (normal, grace, restricted_over_limit, suspended), coleções transacionais `ministry_subscriptions` e `ministry_usage`, middlewares `enforceOperationalAccess` e interface em `/ministerio/plano`. | backend/src/config/plans.config.ts, backend/src/features/subscriptions/, backend/src/middleware/quota-enforcement.ts, web/src/components/SubscriptionPlanView.tsx |
| Billing & Payments | Integração com Asaas implementada no backend (`AsaasBillingProvider`, `BillingService`, `BillingReconcilerWorker`) e frontend (`SubscriptionPlanView`), com suporte a checkouts hospedados v3, webhooks idempotentes, amount validation, transições isoladas em `billing_plan_changes` e future payment cleanup. | backend/src/features/billing/, backend/src/repositories/BillingRepository.ts, web/src/components/SubscriptionPlanView.tsx |
| Backend testing | O backend possui suíte de testes com Vitest cobrindo o motor de quotas, services, controllers, concorrência, idempotência e repositórios. | backend/vitest.config.ts, backend/src/**/*.test.ts |
| Mobile navigation | A barra inferior oferece Início, Escalas, Repertório, Cifras e Ministério; o header expõe troca/criação/entrada no ministério e logout. | BottomNav.tsx, MobileAccountMenu.tsx |
| Authentication bootstrap | Chamadas autenticadas iniciais aguardam authReady e token local válido. | web/src/auth-bootstrap.ts, web/src/App.tsx |
| PWA | Vite gera service worker versionado com precache apenas de shell/assets estáticos; navegações usam NetworkOnly com fallback offline genérico e não são armazenadas. Atualizações dependem de confirmação; não há runtime cache de API. | web/vite.config.ts, web/src/pwa.ts, web/public/offline.html |
| Web testing | Vitest/Testing Library cobrem helpers/componentes e Playwright cobre jornadas móveis com API mockada em seis combinações de viewport/tema. | web/vitest.config.ts, web/src/**/*.test.*, web/playwright.config.ts, web/e2e/ |
| API | Express aplica CORS e JSON globalmente, expõe health/diag e monta rotas em /api/v1. | backend/src/app.ts |
| Authentication | Signup cria usuário em Firebase Auth e perfil users; login emite JWT próprio com duração de sete dias. | AuthService, UserRepository |
| Session | O frontend guarda o JWT na chave localStorage praise_auth_token e o envia como Bearer. | web/src/api.ts |
| Tenancy & IDOR Protection | Repositórios e rotas aplicam isolamento multitenant estrito por ministry_id/group_id, com validação de ownership em todas as operações por ID (repertório, escalas, pastas, artistas, classificações, liturgias). | RepertoireRepository, ScheduleRepository, LiturgyRepository, rbac.ts |
| Smart Chords Security | Rotas de cifras inteligentes exigem autenticação obrigatória e filtram/validam estritamente por user_id. | smart_chord.routes.ts, smart_chord.controller.ts, SmartChordRepository.ts |
| Authentication & Tokens | Login exige verificação criptográfica estrita via Identity Toolkit (sem bypass). Tokens suportam validação dual assíncrona (JWT + Firebase ID Token). | UserRepository.ts, auth.ts |
| Diagnostic Sanitization | /api/diag é sanitizado em produção para não expor Project ID ou identificadores internos. Headers de segurança HTTP e CORS configurável ativos. | backend/src/app.ts |
| Member Unavailability Self-Service | Gestão de indisponibilidade self-service (Phase 6B) com persistência Firestore (`member_unavailabilities`), derivação autoritativa de identidade (`user_id`/`member_id`), modelo civil wall-clock sem Z [starts_at, ends_at), anti-IDOR fail-closed (404), RBAC (member), paginação estável por cursor e UI responsiva. | backend/src/features/availability/, backend/src/repositories/AvailabilityRepository.ts, web/src/components/MemberAvailabilityView.tsx |
| Schedule Availability Conflicts | Detecção de conflitos entre escalas e indisponibilidades (Phase 6C) com duração de escala (15..1440 min, default 120 min), motor puro civil [starts_at, ends_at), busca de candidatos Firestore paginada com lookback de 90 dias, normalização de IDs de participantes, endpoint POST /api/v1/ministries/:ministryId/availability/check-conflicts (admin), omissão estrita de reason, avisos informativos não-bloqueantes e proteção de concorrência com token de geração na UI. | backend/src/features/availability/, backend/src/features/schedules/, backend/src/repositories/AvailabilityRepository.ts, web/src/components/CreateScheduleModal.tsx |
| Administrative Consolidated Availability | Visualização consolidada administrativa de indisponibilidade de integrantes (Phase 6D-1) com escopo restrito de leitura (admin-only), validação rigorosa de período civil até 90 dias, lookback de 90 dias, varredura contínua contra páginas falsamente vazias pós-filtro residual de overlap, limite de segurança de 1000 candidatos (`AVAILABILITY_QUERY_TOO_LARGE`), enriquecimento em lote de nomes dos integrantes sem vazamento cross-tenant, omissão absoluta do motivo (`reason`), cliente web centralizado em `api.ts` e componente responsivo `AdminAvailabilityView.tsx` com presets temporais e imunidade a race conditions. | backend/src/features/availability/, backend/src/repositories/AvailabilityRepository.ts, web/src/components/AdminAvailabilityView.tsx, web/src/components/MinistryView.tsx |
| Manual Member Availability Management | Gestão administrativa delegada de indisponibilidade para integrantes manuais (Phase 6D-2) exclusivamente para membros com `is_manual === true` e `user_id == null`. Separação estrita de Actor (`req.user.id`) e Subject (`member_id`), persistência de `user_id: null`, `management_source: 'admin_manual'`, metadados de auditoria (`created_by_user_id`, `updated_by_user_id`), bloqueio anti-takeover para membros autenticados (`403 AUTHENTICATED_MEMBER_MUTATION_PROHIBITED`), rotas REST canônicas `/members/:memberId` (admin-only), motivo (`reason`) preservado no contexto manual operacional e omitido estritamente em conflitos (6C) e consolidação (6D-1), componente acessível `ManualMemberAvailabilityModal.tsx` integrado ao menu de membros em `MinistryView.tsx` e reuso integral dos índices existentes. | backend/src/features/availability/, backend/src/repositories/AvailabilityRepository.ts, web/src/components/ManualMemberAvailabilityModal.tsx, web/src/components/MinistryView.tsx |
| Organization Foundation & WhatsApp Entitlements | Fundação de Organizações e capacidade comercial WhatsApp (Phase 7B). Agregado `organizations`, governança `organization_members` (owner/admin), vínculo estrito multi-ministério com invariante `organization_id === null`, provisionamento transacional restrito ao proprietário do ministério (`ministry.owner_user_id`), proteção anti-IDOR fail-closed (404), bloqueio de desassociação do ministério âncora (`400 CANNOT_DETACH_BILLING_ANCHOR`) e cálculo de capacidade comercial WhatsApp derivado do catálogo de planos declarativo (Free=0, Pagos=1). | backend/src/features/organizations/, backend/src/repositories/OrganizationRepository.ts, backend/src/features/subscriptions/subscription.service.ts, organization.feature.test.ts |
| WhatsApp Connection Domain & Secret Encryption | Domínio de conexões WhatsApp (Phase 7C). Persistência Firestore (`whatsapp_connections`, `whatsapp_connection_secrets`, `whatsapp_provider_identity_claims`), máquina de estados estrita com matriz 6x6, criptografia AES-256-GCM com AAD vinculada `${orgId}:${connectionId}`, fail-closed para chave inválida/ausente, claims de identidade com lock transacional anti-colisão, gerenciamento de default da organização e atribuição exclusiva de ministério com gating de conexão ativa (DEC-7C-15), preservação de configuração em transitórios, desconexão terminal atômica com purge de segredo e claim, composição de capacidade comercial e restrição over-limit funcional sem side-effects no banco, paginação por cursor lookahead estável (`pageSize + 1`), rotas REST `/organizations/:organizationId/whatsapp/connections` e `/ministries/:ministryId/whatsapp/status`, omissão absoluta de credenciais/segredos e índices declarados em `firestore.indexes.json`. | backend/src/features/whatsapp/, backend/src/repositories/WhatsAppConnection*, whatsapp.feature.test.ts, firestore.indexes.json |
| WhatsApp Onboarding & Provider Credential Acquisition | Backend de onboarding WhatsApp da Meta Cloud API (Phase 7D1/7D1-R1/7D1-R2/7D1-R3/7D1-B-R3-R1; contratos de liveness e recuperação Phase 7D1-A-R3 até Phase 7D1-A-R15 formalizados eliminando retenção de capacidade pré-materialização, reconciliando ortogonalidade entre status de sessão e progresso de provedor, liberando capacidade imediatamente na expiração rígida de 24h desacoplada de compensação assíncrona, coordenando ciclo de vida de assinaturas de webhooks compartilhados em plataforma via coordenador de estado desejado em `whatsapp_waba_lifecycle_locks` (`lock_${provider}_${provider_waba_id}`) com leases de 120s refatorados estritamente como margem de segurança de execução local entre contêineres sobre runtime de função de 60s sem presunção de cancelamento remoto de requisições Meta, modelo explícito de incerteza de mutação remota (`NOT_STARTED`, `IN_FLIGHT`, `CONFIRMED_SUCCESS`, `CONFIRMED_FAILURE`, `UNKNOWN_OUTCOME`), rastreamento em livro-razão de incertezas remotas multi-geração (`unresolved_remote_mutations`), semântica de snapshot com proveniência (`provider_observed_at`, `provider_observed_generation`) onde uma leitura isolada de ausência/presença não liquida incertezas passadas, protocolo de reasserção ativa (`Active Re-Assertion Protocol`) para onboarding e cleanup restabelecendo a operabilidade do estado atual sem presumir cancelamento do passado remoto, cadência de observação de reconciliação de 300s sem constantes arbitrárias de liquidação temporal, retenção segura de segredos em modo fail-closed proibindo purga de credenciais sob dívida de subscrição ativa, protocolo de convergência para o estado desejado da geração mais recente rejeitando commits de gerações obsoletas, verificação de convergência pré-conexão exigindo `provider_observed_state === 'subscribed'` antes da materialização de conexão, rechecagem obrigatória de dependências ativas pós-aquisição de lease de WABA, fechando contradições de resultado do provedor com distinção estrita entre proven-clean e unproven-clean com verificação autoritativa pós-condição via `GET /{waba_id}/subscribed_apps` com paginação exaustiva obrigatória navegando cursores seguros contra SSRF onde ausência só é declarada após varredura de toda a coleção (truncamento por limite operacional resulta estritamente em `UNPROVEN_CLEAN`), retenção de credencial e supressão de TTL para incidentes não resolvidos, definindo executor protegido por leases com modelo de descoberta de duas queries para recuperação de leases expirados, reserva transacional pré-chamada de tentativas garantindo teto estrito de chamadas externas sem duplo incremento pós-chamada, fencing de workers obsoletos por token de lease, transporte GET com `Cache-Control: no-store` compatível com cron-job.org (DURABLE_EXECUTOR: CRON_JOB_ORG, EXECUTOR_CLASS: EXTERNAL_BEST_EFFORT_SCHEDULER, GOOGLE_CLOUD_BILLING_REQUIRED_FOR_SCHEDULER: NO, SCHEDULER_COST_CURRENT_STAGE: R$0, SCHEDULER_JOB_COUNT: 2_GLOBAL, VERCEL_PLAN: HOBBY, VERCEL_CRON: NOT_USED_FOR_PHASE_7D1, SCHEDULE: EVERY_5_MINUTES) autenticado via `CRON_SECRET` com verificação de igualdade em tempo constante por digests SHA-256 em tokens bearer bem formados, família canônica de rotas `/api/v1/internal/whatsapp/cleanup-jobs/...` com separação de autoridade entre agendamento máquina (`CRON_SECRET`) e remediação humana via autoridade de operador de plataforma (`INTERNAL_OPERATOR_SECRET`), atribuição inviolável de identidade de operador derivada exclusivamente do contexto de autenticação (`manual_action_by = 'internal_operator'`) proibindo falsificação via corpo da requisição, semântica explícita de `force_abandon: true` com justificativa obrigatória para descarte de incidentes com credenciais revogadas onde prova do provedor é impossível, resposta de contenção padronizada como HTTP 409 `WABA_LIFECYCLE_CONTENTION` com `Retry-After: 5`, versionamento dinâmico centralizado de Graph API via `unifiedConfig.metaGraphApiVersion` com validação de formato regex `^v[0-9]+(\.[0-9]+)?$`, orçamento interno suave de 20s e corte de aquisição em 15s com pré-requisito de runtime de função efetivo >= 60s em `vercel.json` distinguindo Fluid Compute de limites legados e trava de compatibilidade de AAD Phase 7C com retenção lógica de 30 dias; endurecimento de envelope de execução de 30 segundos Phase 7D1-B-R3-R1 via autoridade `WhatsAppExecutionDeadline` [orçamento de 24s com margem de segurança de 1.5s] com grampeamento dinâmico de timeouts externos `getClampedTimeoutMs`, guarda pré-reserva de tentativas em < 3.000ms dispensando execução remota com `INSUFFICIENT_EXECUTION_BUDGET` e liberando lease como `idle` sem queimar tentativas, guarda de paginação em < 1.000ms retornando `UNPROVEN`, e guarda de verificação pós-condição em < 2.000ms marcando `VERIFICATION_BUDGET_EXHAUSTED` com registro de `unknown_outcome` para reconciliação autônoma). Alocação de conexões pendentes (`pending`) com prazo rígido de 24 horas (`pending_expires_at = createdAt + 24h`) e sessões efêmeras (`whatsapp_onboarding_sessions`) de 15 minutos lógicos e retenção física limitada de 30 dias (`retention_expires_at = createdAt + 30d`) com barreira lógica de expiração na aplicação para suporte a replay idempotente de conclusão, verificação CSRF em tempo constante via hash SHA-256 (`state_nonce_hash`), reserva atômica de capacidade com contenção OCC em `organizations`, gating estrito exigindo `billingAccessMode === 'normal'` para novas reservas e permissão de conclusão/retomada estagiada durante grace (DEC-7D-16), transição atômica imediata de reservas pendentes expiradas para `disconnected` (`PENDING_EXPIRED`) liberando instantaneamente a cota comercial da organização e delegando compensação externa de webhooks a jobs assíncronos duráveis em `whatsapp_provider_cleanup_jobs` (`cleanup_conn_${connId}`) executados via rota interna canônica protegida `GET /api/v1/internal/whatsapp/cleanup-jobs/execute` (autenticada via `CRON_SECRET` com comparação por digests SHA-256, `Cache-Control: no-store`, acionada por cron-job.org a cada 5 minutos em produção ou worker em dev com orçamento interno suave de 20s e corte de aquisição em 15s suportado por duração efetiva de função >= 60s e envelope de resposta <= 25s) com descoberta por duas queries (prontos e leases `processing` expirados), leases transacionais de 5 minutos, reserva transacional pré-chamada de tentativas (`attempt_count`) garantindo invariante de teto estrito contra crashes (DEC-7D-50), fencing de escrita por `lease_token`, retry exponencial (5 tentativas em 24h), classificação estrita entre `PROVEN_CLEAN` (`succeeded`: HTTP 200 `{ ok: true, ... }` ou confirmação autoritativa pós-condição via `GET /{waba_id}/subscribed_apps` com paginação exaustiva segura por cursores inspecionando `whatsapp_business_api_data.id`, expurgo de segredo, TTL 30d), `NO_PROVIDER_CLEANUP_NEEDED` (`cancelled`: expurgo de segredo, TTL 30d) e `UNPROVEN_CLEAN` (`exhausted`: 5 tentativas esgotadas, erro em paginação, malformação, 401/190 ou 403, segredo criptografado RETIDO em `whatsapp_connection_secrets`, `retention_expires_at = null` para supressão de TTL físico e emissão de alerta operacional para intervenção humana através de costuras administrativas canônicas `/api/v1/internal/whatsapp/cleanup-jobs/:jobId/retry` e `/abandon`; proibido expurgar segredo em respostas ambíguas) (DEC-7D-41, DEC-7D-43, DEC-7D-51, DEC-7D-52, DEC-7D-53, DEC-7D-54, DEC-7D-55, DEC-7D-56, DEC-7D-57, DEC-7D-58, DEC-7D-59, DEC-7D-60, DEC-7D-61, DEC-7D-62, DEC-7D-63, DEC-7D-64, DEC-7D-65, DEC-7D-66, DEC-7D-67, DEC-7D-68, DEC-7D-69), livro-razão de incerteza remota multi-geração limitado a 20 registros (`unresolved_remote_mutations`) substituindo dívida singular e eliminando constantes temporais arbitrárias de prova (300s é estritamente cadência de observação de reconciliação durável sem jamais apagar dívidas do provedor sem prova autoritativa ou override manual `force_abandon`), protocolo de reasserção ativa redefinido como reparo de operabilidade do estado atual sem presumir cancelamento do passado remoto, reconciliador durável em segundo plano `whatsapp_waba_reconciliation_jobs` (`recon_meta_${wabaId}`) disparado com zero ações futuras do usuário com backoff exponencial (1m, 5m, 15m, 1h, 6h), coexistência de desconexão com liberação imediata de cota comercial e retenção fail-closed de credenciais criptografadas em `whatsapp_connection_secrets` enquanto houver dívida de subscrição na WABA e desacoplamento formal da correção contra durações de runtime serverless Vercel, purga de segredos com AAD canônica Phase 7C `${organization_id}:${connection_id}` (DEC-7D-49), liberação atômica imediata de reservas pendentes não materializadas (`disconnected`) em falhas pré-materialização terminais (CSRF inválido, suspensão prévia, falha na troca de código OAuth, WABA/telefone não autorizados, claim colidida) evitando bloqueio de 24 horas, saga de 13 passos para aquisição e estágio progressivo de credenciais criptografadas (`token_type: 'business_token'`) com separação ortogonal estrita de `session.status` (`'active' | 'consumed' | 'expired' | 'failed'`) e `provider_progress` mantendo a sessão em `active` durante os passos intermediários (DEC-7D-38), verificação de autoridade no Messaging Account Container (`verifyMessagingAccountAccess`) e edge de telefones (`listAuthorizedPhoneNumbers`), normalização estrita E.164, higiene precisa de memória V8/Node (zero persistência/logging, Buffers mutáveis zerados em melhor esforço), assinatura de webhooks coordenada via coordenador de estado desejado `whatsapp_waba_lifecycle_locks` (`lock_meta_${providerWabaId}`) com leases de 120s, cancelamento de limpeza se dependências > 0 protegendo contra desinscrição concorrente de WABA compartilhado, isolamento de chamadas externas Meta fora de transações Firestore, fronteiras estritas de efeitos colaterais (`GET /connections` estritamente somente-leitura com transformação em memória de pendentes expirados e `POST /start` com transações exclusivamente locais), rechecagem transacional autoritativa de capacidade comercial e modo de acesso no Passo 10 abortando antes de qualquer aquisição de claim em caso de downgrade/estouro de cota com expurgo imediato de segredo estagiado (ou retenção não-destrutiva para regularização de assinatura com retomada via `POST /start` com `resumeConnectionId` em `mode = 'resume_staged'`), guarda contra sessões obsoletas (`ONBOARDING_SESSION_SUPERSEDED`), idempotência em replay de conclusão retornando 200 com DTO sanitizado, materialização atômica em transação com claim determinístico `claim_meta_${phoneNumberId}`, prevenção de credenciais órfãs e retenção de segredo para retry seguro em falhas transitórias, rotas REST `POST /organizations/:orgId/whatsapp/onboarding/start` e `POST /organizations/:orgId/whatsapp/onboarding/complete`. (META_LIVE_E2E_VALIDATION: NOT_EXECUTED; validado via suíte de testes Vitest com mocks de rede e isolamento estrito de Firestore em emulador local Phase 7D1-C-R1 via `assertTestIsolationGuard` com garantia de zero escrita em produção). | backend/src/features/whatsapp/meta-whatsapp.provider.ts, backend/src/features/whatsapp/whatsapp-connection.service.ts, backend/src/repositories/WhatsAppOnboardingSessionRepository.ts, organization.routes.ts, meta-whatsapp.provider.test.ts, whatsapp.onboarding.test.ts, backend/src/lib/firebase.ts, backend/src/lib/firebase-test-isolation.test.ts |
| WhatsApp Commercial Entitlement | Concessão e fiscalização de capacidade comercial WhatsApp (Phase 7D2-D8 / 7D2-D8-R1 / 7D2-D8-R3). Avaliador comercial puro `evaluateWhatsAppCommercialEntitlement` como autoridade comercial única e centralizada consumindo a cadeia canônica Billing V1 (`Organization` -> `billing_anchor_ministry_id` -> documento de `Ministry` -> `ministry_subscriptions` / `SubscriptionService`), catálogo declarativo de planos com cota exclusiva para plano Premium (Premium = 1 conexão incluída; Free/Lite/Lite+/Essential/Pro = 0; conexões adicionais = 0), eliminação de autoridades paralelas e modelos sintéticos de fallback em `SubscriptionService` onde ausência de subscrição sob âncora resulta em `integrity_failure` (`COMMERCIAL_INTEGRITY_VIOLATION`), validação estrita de data civil de carência (`parseCanonicalCivilBillingDate`) em `America/Sao_Paulo` com fronteira semiaberta `[start, end)` de 7 dias civis (`grace_period_expires_billing_date`) rejeitando strings malformadas, timestamps e datas inexistentes, contagem de capacidade exata e limitada via consultas particionadas no Firestore (`getConsumingConnections`: Partição 1 para status operacionais `connecting`, `connected`, `error`, `disabled_by_user` limit 50; Partição 2 para status `pending` ordenada deterministicamente por `FieldPath.documentId()` em páginas bounded de 50 até 10 páginas / 500 registros, com saturação fail-closed 429 `CAPACITY_ACCOUNTING_SATURATED`), aplicação estrita da política de expiração malformada (apenas ISO string válida no passado libera capacidade; ausente, nulo, whitespace, não-string ou inválido consome fail-closed até intervenção operacional), admissão comercial server-owned no onboarding (`verifyServerOwnedAdmission`) via prova durável de 11 pontos sem exigência de progresso/segredos de provedor e liberando retomada autorizada de 24h durante carência (`canResumeAuthorizedOnboarding`), limpeza lazy transacional de pendentes expirados no início de onboarding com precedência estrita de leituras sobre escritas, linearização atômica pré-envio em `WhatsAppOutboundDispatchRepository.acquireDispatchExecution` com 100% de paridade de fatos com `SubscriptionService` delegando a contagem ao repositório central de conexões, isolamento multi-tenant estrito com anti-IDOR fail-closed (404/403), retrocompatibilidade de DTOs (`OrganizationWhatsAppCapacity` e `OrganizationWhatsAppCapacityUsageDto`) via adaptadores puros e retenção exclusiva dos índices necessários em `firestore.indexes.json` (`organization_id ASC, status ASC`). | backend/src/features/subscriptions/whatsapp-commercial-evaluator.ts, backend/src/features/subscriptions/subscription.service.ts, backend/src/features/whatsapp/whatsapp-connection.service.ts, backend/src/repositories/WhatsAppConnectionRepository.ts, backend/src/repositories/WhatsAppOutboundDispatchRepository.ts, firestore.indexes.json |
| WhatsApp Commercial-State UX | Experiência de estado comercial WhatsApp na PWA baseada estritamente no contrato autoritativo D8 do backend (Phase 7E-F4). Componentes dedicados `WhatsAppCommercialStatusBanner` e `WhatsAppCapacityCard`, cobertura dos 7 estados canônicos (`healthy`, `payment_grace`, `post_payment_grace`, `plan_excluded`, `administratively_suspended`, `restricted_over_limit`, `integrity_failure`), formatação de data civil (`formatDatePtBR`) imune a desvios de fuso horário, supressão de termos técnicos internos em falhas de integridade, CTAs contextuais (regularização, planos, suporte) e isolamento seguro de navegação compatível com suítes de testes unitários sem contexto de Router. | web/src/components/whatsapp/WhatsAppCommercialStatusBanner.tsx, web/src/components/whatsapp/WhatsAppCapacityCard.tsx, web/src/components/WhatsAppFoundationView.tsx, web/src/whatsapp-errors.ts, web/src/whatsapp.feature.test.tsx, web/e2e/whatsapp-commercial.spec.ts |
| WhatsApp Connection Management UI | Experiência de gestão de conexões WhatsApp na PWA para administradores da organização (Phase 7E-F3 / Remediação Phase 7E-F3-R1). Componentes `WhatsAppConnectionList`, `WhatsAppConnectionCard` e modal de confirmação `WhatsAppDisconnectModal`; RBAC estrito com isolamento total para integrantes comuns (`isAdmin === false`) que nunca disparam requisições para `/whatsapp/connections` ou `/entitlements/whatsapp`; fluxo de paginação íntegro preservando `nextCursor` da primeira página autoritativa em `WhatsAppFoundationView` e propagando `initialNextCursor` para `WhatsAppConnectionList`; paginação por cursor (`limit: 10`) com deduplicação determinística por `connection.id` (`deduplicateConnections`); proteção contra race conditions e vazamento multi-tenant via fencing de geração (`generationRef`), `organizationIdRef`, `ministryIdRef`, `inFlightCursorRef` e `isMountedRef` em carregamento de páginas subsequentes, listagem inicial e mutações; edição inline de nome de exibição (1..100 caracteres com trim e feedback visual), alternância de conexão padrão (`isOrganizationDefault`) com desmarcação atômica local, atribuição exclusiva para ministério (`assignedMinistryId`), gating de ações para conexões ativas (`CONNECTION_NOT_ACTIVE_FOR_CONFIGURATION`, `CANNOT_SET_ASSIGNED_CONNECTION_AS_DEFAULT`, `CANNOT_ASSIGN_DEFAULT_CONNECTION`); desconexão terminal confirmada com liberação de capacidade, atualização de status para `disconnected` e gating do botão Conectar até que o recarregamento autoritativo de capacidade reflita a liberação (`canCreateConnection: true`); estados explícitos de `loading`, `empty`, `error` com retry ("Tentar novamente") e `success`, touch targets >= 44px e descarte seguro de respostas assíncronas obsoletas na alternância de ministério/organização, com fechamento de modais abertos. | web/src/components/whatsapp/WhatsAppConnectionList.tsx, web/src/components/whatsapp/WhatsAppConnectionCard.tsx, web/src/components/whatsapp/WhatsAppDisconnectModal.tsx, web/src/components/WhatsAppFoundationView.tsx, web/src/whatsapp.feature.test.tsx, web/e2e/whatsapp-connections.spec.ts |


## Confirmed Inconsistencies

| ID | Area | Inconsistency and impact | Evidence |
| --- | --- | --- | --- |
| INC-001 | Route aliases | O diretório features/groups implementa GroupRepository e group routes, mas app.ts não monta groupRoutes. /api/v1/groups monta ministryRoutes, portanto o módulo groups dedicado fica inalcançável e o alias usa coleções ministry_*. | backend/src/app.ts, features/groups/, MinistryRepository.ts |
| INC-005 | Smart Chords endpoints | RESOLVIDO (Fase 5B): Rotas canônicas unificadas em /api/v1/smart-chords, GET /song/:songId com precedência estrita sobre /:id, eliminação de endpoints fabricados (/smart-chords/song/:songId para mutação) e alinhamento de contratos. | web/src/api.ts, smart_chord.routes.ts, smart_chord.controller.ts |

INC-002 e INC-003 foram corrigidos na implementação do sistema de planos. INC-004, INC-006, INC-007, INC-008, INC-009, INC-010, INC-011 e INC-012 foram integralmente resolvidos na fase de Authentication & Authorization Security Hardening (2026-08-29). GAP-011 (Asaas Customer Reuse) foi integralmente implementado, protegido contra concorrência e revalidado com sucesso em Sandbox (2026-09-01). GAP-004 (Liturgies navigation) foi resolvido na Fase 5A (2026-09-09), expondo LiturgiesView via rota canônica /liturgias, sidebar desktop e Ministério no mobile. INC-005 (Smart Chords endpoints) e GAP-003 (Smart Chords persistence & list) foram resolvidos na Fase 5B (2026-09-09), integrando frontend ao backend real com isolamento estrito por usuário, anti-IDOR e validação de vínculo com repertório. GAP-005 (Dashboard announcements) foi resolvido na Fase 5C (2026-09-09), substituindo mocks locais por persistência Firestore multi-tenant, RBAC (membro leitura, admin CRUD), anti-IDOR fail-closed e UX responsiva.

## Incomplete Implementation / Known Gaps

| ID | Area | Current state | Evidence |
| --- | --- | --- | --- |
| GAP-002 | Lint | Backend declara eslint src/, mas não possui ESLint em dependencies/devDependencies nem arquivo de configuração. Web não declara lint. | backend/package.json, inventário |
| GAP-003 | Smart Chords list | RESOLVIDO (Fase 5B): api.getSmartChords integrado a GET /api/v1/smart-chords com persistência real Firestore, isolamento por user_id, mapeamento de campos e correção de colisão empty/error. | web/src/api.ts, SmartChordRepository.ts, SmartChordsWorkspace.tsx |
| GAP-004 | Liturgies navigation | RESOLVIDO (Fase 5A): LiturgiesView exposto na rota /liturgias, sidebar desktop e card Ministério no mobile; RBAC e isolamento de tenant preservados. | App.tsx, LiturgiesView.tsx, routing.ts |
| GAP-005 | Dashboard announcements | RESOLVIDO (Fase 5C): MOCK_ANNOUNCEMENTS substituído por persistência Firestore (`ministry_announcements`), rota canônica `/api/v1/ministries/:ministryId/announcements`, RBAC estrito (membro read-only, admin CRUD), anti-IDOR fail-closed, queries ordenadas/limitadas e modais acessíveis. | DashboardView.tsx, announcement.routes.ts, AnnouncementRepository.ts |
| GAP-006 | Supabase migration residue | @supabase/supabase-js permanece como dependência; env.ts expõe campos vazios e lib/supabase.ts retorna null. | backend/package.json, config/env.ts, lib/supabase.ts |
| GAP-008 | Infrastructure | Não há CI/CD, Docker, migrations ou schema Firestore versionado. | inventário |
| GAP-009 | Formatting | Não há formatter ou script format configurado. | package.json, inventário |
| GAP-010 | Dependency advisories | A instalação das dependências web reportou vulnerabilidades em dependências indiretas. | saída de npm install; Unknown / Not yet verified |
| GAP-012 | Same-Plan Interval Change & Transition Policy | Reconhecimento de ciclo e UI validados em Sandbox; execução financeira requer reengenharia para agendamento em `current_period_end` sem sobreposição de cobranças integrais conforme Política V1 (ADR 2026-09-01; APPROVED DOMAIN POLICY — IMPLEMENTATION PENDING). | `SubscriptionPlanView.tsx`, `BillingService.ts` |

## Outdated Documentation

| Previous claim | Current evidence |
| --- | --- |
| O produto atual inclui mobile/ Flutter. | Não existe diretório mobile/ no checkout. |
| A persistência ativa é Supabase/PostgreSQL. | Repositories importam Firestore; lib/supabase.ts está marcado deprecated e retorna null. |
| Existem migrations SQL sob supabase/migrations/. | Não existe diretório supabase/. |
| O repositório contém somente backend/mobile. | O cliente ativo está em web/ com React/Vite/PWA. |
| O backend não tem testes. | Backend possui suíte de testes Vitest em `backend/src/`. |
| Não há provedor de pagamento ou worker de background. | Integração Asaas e `BillingReconcilerWorker` implementados. |

README.md, AGENTS.md e GEMINI.md foram alinhados para não perpetuar essas afirmações.

## Unknown / Not yet verified

- Produção ativa, URLs publicadas e saúde dos deployments.
- Regras de segurança, índices, TTLs, backups e políticas de retenção do Firestore.
- Existência/estado das coleções em qualquer projeto Firebase real de produção.
- Metas de disponibilidade, tráfego, volume de dados ou desempenho.
- Responsável operacional, processo de release e estratégia de rollback.
- Versão mínima oficialmente suportada de Node.js e npm.
- Concorrência de transações sob Firestore Emulator: NOT YET VERIFIED (atomicidade e isolamento validados em testes unitários com mocks via Vitest; emulador físico do Firestore indisponível no host).
- Integração Asaas: A integração foi implementada com os fluxos principais homologados em ambiente Sandbox do Asaas (GAP-011 revalidado; Política V1 de transições agendadas aprovada com implementação pendente). Configurações, credenciais e deploy de produção não devem ser presumidos.

## Phase 7E — WhatsApp PWA Code Closure Status

**PHASE 7E WHATSAPP PWA CODE CLOSURE: COMPLETE** — No remaining code blockers. All confirmed Phase 7E capabilities implemented:

- Meta/Zernio onboarding;
- callback/refetch;
- D8 commercial-state UX;
- connection management;
- disconnect;
- pagination;
- admin/member RBAC separation;
- tenant-generation stale-response protection;
- automatic-notification-only V1 scope.

### Operational Homologation Items (NOT COMPLETE)

The following are operational homologation items requiring environment validation, **not code defects**:

- Real Meta Embedded Signup popup-source behavior has not yet been live verified;
- Meta provider credentials/configuration require environment validation;
- Zernio provider credentials/configuration require environment validation;
- Callback URLs require environment validation;
- Required Firestore indexes must be deployed/verified before production traffic.

**Do NOT mark operational homologation as complete. Do NOT claim Meta live behavior is verified.**

## Use in Future Work

Ao tocar uma área listada:

1. confirme novamente a evidência, pois o código pode ter mudado;
2. crie um ExecPlan para correções não triviais;
3. trate segurança/autorização como mudança de contrato;
4. adicione testes deliberadamente como parte do escopo da correção;
5. mova o item para comportamento confirmado somente depois de validar a solução.
